# TADX V1 Capability Contract

**Document status:** Consolidated contract - architecture locked; Pulse lifecycle live-verified
**Prepared:** 2026-08-29  
**Product / binary:** `tadx`  
**Canonical scope:** Public and discoverable V1 operations, shared execution invariants, CLI implementation evidence, boundaries, and implementation-blocking verification.

## 1. Status, sources, and validation levels

### 1.1 Source register and precedence

| Label | Attached source | Version / date | Authority in this contract |
| --- | --- | --- | --- |
| **A1** | `tableau-agent-development-harness-arc42-locked.md` | Architecture complete; last reconciled 2026-08-29 | Authoritative for product behavior, ownership, safety, V1 scope, and architecture boundaries. |
| **C1** | `tadx-v1-capability-matrix-current.md` | Current draft; prepared 2026-08-29; validation pending | API research and implementation evidence. It is not architecture authority. |
| **S1** | `Pasted markdown.md` | Consolidation review supplied 2026-08-29 | Authoritative for the requested document shape and structural corrections applied here. |

Precedence is **A1**, then **S1** for consolidation structure, then **C1** for API facts. This contract does not silently replace an A1 decision with an endpoint-driven inference.

C1 refers to `R1`, `M1`, `M2`, `V1`, `X1`, `X2`, and `X3`, but those underlying documents, exact URLs, OpenAPI artifacts, operation IDs, and captured versions are not attached. Consequently:

- C1-derived remote API facts are **docs-only** evidence in this contract.
- Pulse lifecycle capabilities are **live-verified** through the evidence recorded in `docs/evidence/pulse-live-contract.md`.
- A remote capability cannot move to **live-verified** until its exact official source and required contract and live tests are attached to the implementation record.
- Other remote capabilities retain their existing evidence level.

### 1.2 Validation levels

| Level | Meaning |
| --- | --- |
| **Architecture-locked** | A1 fixes the user-visible behavior or boundary. Implementation must preserve it. |
| **Local contract** | Deterministic local behavior is fixed by A1 and does not depend on a Tableau API fact. |
| **Docs-only** | C1 states an upstream operation or behavior, but the exact official source capture and live evidence are not present in the attachment set. |
| **Contract-verified** | An exact official source capture and passing local contract tests establish the request, response, and error boundary. Live deployment evidence is not claimed. |
| **Live-verified** | Exact official source plus a captured successful or negative contract test on a supported deployment. |
| **Blocked** | A V1 commitment cannot be completed safely until the named verification gate in §6 closes. |
| **Delegated** | Discoverable through the registry, but executed by MCP, Tableau/Desktop, or an agent/skill rather than TADX CLI. |
| **Deferred / rejected / out of scope** | Not a V1 executable registry operation. The boundary is recorded in §5. |

A `Blocked` row is still a V1 product commitment; it is not implementation-ready. A `Ship` row with `Docs-only` evidence is admitted but still requires ordinary source capture and contract testing before merge.

### 1.3 Canonicality rule

Section 2 is the sole hand-authored operation inventory. Generated help, ownership views, public command trees, and capability documentation must be derived from the executable registry represented by these rows. Internal adapter calls do not receive public capability rows merely because an endpoint exists.

## 2. Canonical V1 capability registry

The following tables are one logical registry split only for readability. Every row represents exactly one public CLI operation or one delegated operation that `capability get` must be able to describe.

Registry totals: **97 rows** - **84 CLI-owned** and **13 delegated**; **84 Ship** and **13 Delegated**.

Field rules:

- `Local write`, `Remote mutation`, and `Supports --preview` are independent.
- Remote mutation commands and capability rows are always discoverable.
- `TADX_ENABLE_MUTATIONS=1` enables commands whose `Remote mutation` is `Yes`.
- Enabled mutation commands run by default and support `--preview` for a read-only plan.
- `Artifact effect` concerns managed Tableau resource artifacts, not config/catalog housekeeping.
- Deferred, rejected, and out-of-scope boundaries are not duplicated here; they are listed once in §5.

### 2.1 Foundation, discovery, and local state

| Capability ID | Public command or delegated surface | Resource and user outcome | Type | V1 status | Owner | MCP overlap | Selectors | Products / availability | Local write | Remote mutation | Supports `--preview` | Safety / guard | Artifact effect | Upstream operation | Evidence | Validation / blocker |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| agent.install | `tadx agent install` | Install the bundled TADX Guidance packages into the selected agent's global skill directory. | Change | Ship | CLI | - | Required target: claude, codex, or cursor | Local / all | Yes | No | Yes | Bounded home-relative paths; symlink rejection; staged package replacement; divergent packages require --force and retain backups; no instruction or rules files | None | Embedded packages and local filesystem | Standard Agent Skills package format; official agent skill discovery directories | Local contract verified by installer and CLI tests |
| agent.uninstall | `tadx agent uninstall` | Remove TADX Guidance packages from one selected agent target. | Change | Ship | CLI | - | Required target: claude, codex, or cursor | Local / all | Yes | No | Yes | Exact bounded package paths; symlink rejection; divergent installed Guidance requires `--force` and retains a backup | None | Local agent Guidance directories | Standard Agent Skills package format; installer manifest and local filesystem | Local contract verified by uninstaller and CLI tests |
| version.get | `tadx version` | Report the installed TADX version and optionally check the latest published release. | Inspect | Ship | CLI | - | Optional `--check` | Local / all; release check requires GitHub access | No | No | No | Offline by default; optional check performs one bounded request and never changes the binary | None | Build metadata; optional GitHub latest-release endpoint | Go build information and bounded HTTP contract | Local contract verified by version and CLI tests |
| env.profile.list | `tadx env list` | List named non-secret environment profiles. | Find | Ship | CLI | - | Optional config path | Local / all | No | No | No | Secret redaction | None | `config.yaml` under `os.UserConfigDir()/tadx` | A1 §§1.5, 7.2–7.4, 9.2; C1 §2.1 | Architecture-locked local contract |
| env.profile.get | `tadx env get` | Inspect one resolved non-secret environment profile. | Inspect | Ship | CLI | - | Environment alias | Local / all | No | No | No | Secret redaction; exact alias | None | Read local `config.yaml` | A1 §§5.3, 7.2–7.4; C1 §2.1 | Architecture-locked local contract |
| env.profile.add | `tadx env add` | Add one named environment profile containing secret references, not secret values. | Change | Ship | CLI | - | New environment alias | Local / all | Yes | No | No | Schema validation; collision guard; atomic file replacement | None | Write local `config.yaml` | A1 §§7.2–7.4, ADR-010; C1 §2.1 | Architecture-locked local contract |
| env.profile.update | `tadx env update` | Update explicit fields of one environment profile. | Change | Ship | CLI | - | Environment alias | Local / all | Yes | No | No | Exact alias; secret redaction; atomic file replacement | None | Write local `config.yaml` | A1 §§7.2–7.4; C1 §2.1 | Architecture-locked local contract |
| env.profile.remove | `tadx env remove` | Remove one named environment profile. | Change | Ship | CLI | - | Environment alias | Local / all | Yes | No | No | Exact alias; default-reference guard | None | Write local `config.yaml` | A1 §§7.2–7.4; C1 §2.1 | Architecture-locked local contract |
| env.profile.set-default | `tadx env default` | Set the default read environment. | Change | Ship | CLI | - | Environment alias | Local / all | Yes | No | No | Alias must exist; no secret output | None | Write `default_environment` in local `config.yaml` | A1 §§1.5, 7.2–7.3, 9.2; S1 “one capability row per public operation” | Architecture-locked local contract |
| auth.check | `tadx auth check` | Resolve PAT references, sign in, and verify the selected Tableau site. | Inspect | Ship | CLI | - | Environment alias; site content URL | Cloud / Server | No | No | No | Never persist or echo PAT/token | None | `POST /api/{version}/auth/signin`; optional signout | A1 §§5.4, 7.3–7.4; C1 §§2.1, 5.1; local official REST capture | Contract-verified; exact source and tests recorded in `docs/evidence/phase1-rest-contract.md` |
| auth.status | `tadx auth status` | Report resolved auth configuration and PAT-reference presence without revealing values. | Inspect | Ship | CLI | - | Optional environment alias | Local / all | No | No | No | Does not claim remote validity; secret redaction | None | Local config/environment resolution | A1 §§1.5, 5.4, 9.2; S1 notes missing distinct row | Architecture-locked; remote-validity semantics intentionally belong to `auth check` |
| capability.list | `tadx capability list` | Return a bounded inventory of discoverable operations, ownership, and execution availability. | Find | Ship | CLI | - | Domain/resource/owner/product/mutation filters | Local / all | No | No | No | Always discoverable; mutation rows report whether execution is enabled | None | Executable capability registry | A1 §§1.5, 3.3, 5.2, 6.6; C1 §2.1 | Architecture-locked local contract |
| capability.get | `tadx capability get <id>` | Return focused execution, ownership, selector, safety, and availability guidance for one capability. | Inspect | Ship | CLI | - | Stable capability ID | Local / all | No | No | No | Exact ID; includes delegated surfaces | None | Executable capability registry | A1 §§3.3, 5.2; C1 §2.1 | Architecture-locked local contract |
| catalog.refresh | `tadx catalog refresh` | Hydrate and replace one normalized site inventory generation. | Inspect | Ship | CLI | - | Environment/site; admitted scopes | Cloud / Server | Yes | No | No | Incomplete generations never become current | None | Admitted REST list and permissions endpoints | `docs/evidence/group2-inventory-rest-contract.md` | Contract-verified eight-scope concurrent hydration and transactional SQLite publication |
| catalog.status | `tadx catalog status` | Report generation age, completeness, source, and stale state. | Inspect | Ship | CLI | - | Optional environment/site | Local / all | No | No | No | 12-hour stale warning; stale is not invalid | None | Local catalog generation metadata | A1 §§5.9, 12.8; C1 §2.1 | Architecture-locked local contract |
| workspace.create | `tadx workspace create` | Create a named workspace with `tadx.yaml`, `artifacts/`, and `.tadx/` under &lt;home&gt;/TADX/workspaces/&lt;name&gt; or an explicit `--path`. | Change | Ship | CLI | - | Name; optional path override | Local / all | Yes | No | No | Portable name, identity, root collision, and path-boundary checks; no implicit creation by pull; registered root appears only under `--full` | None | Local filesystem | A1 §§6.1, 7.5–7.6, 9.2; C1 §2.1 | Architecture-locked local contract |
| workspace.register | `tadx workspace register` | Adopt an existing on-disk workspace directory into the local registry using its tadx.yaml identity. | Change | Ship | CLI | - | Path/name | Local / all | Yes | No | No | Requires a valid tadx.yaml; name/ID/path collision checks; adopts existing identity, no directory mutation | None | Local filesystem | A1 §§6.1, 7.5–7.6, 9.2; C1 §2.1 | Architecture-locked local contract |
| workspace.clone | `tadx workspace clone` | Copy an existing managed workspace under a new identity at &lt;home&gt;/TADX/workspaces/&lt;name&gt; or an explicit `--path`. | Change | Ship | CLI | - | Source/name; optional path override | Local / all | Yes | No | No | Rejects existing destination and symlinked entries; portable name; new identity; name/ID/path collision checks; registered root appears only under `--full` | None | Local filesystem | A1 §§6.1, 7.5–7.6, 9.2; C1 §2.1 | Architecture-locked local contract |
| workspace.list | `tadx workspace list` | List registered named workspaces. | Find | Ship | CLI | - | Bounded registered-workspace page | Local / all | No | No | No | No unbounded filesystem scan; machine-local roots appear only under `--full` | None | Local config/filesystem | A1 §§6.1, 7.2, 9.2; C1 §2.1 | Architecture-locked local contract |
| workspace.status | `tadx workspace status` | Report effective workspace, artifact state, provenance, dirty/missing status, and the registered root under `--full`. | Inspect | Ship | CLI | - | `--workspace` or deterministic resolution chain | Local / all | No | No | No | No locking; concurrent races are caller responsibility; machine-local root appears only under `--full` | Read | Fingerprint managed local content | A1 §§6.1, 6.3–6.4, 8.2, ADR-032; C1 §2.1 | Architecture-locked; C1 lock suggestion rejected |
| workspace.move | `tadx workspace move` | Move one local artifact without changing Tableau identity. | Change | Ship | CLI | - | Source and destination logical workspace names plus workspace-relative artifact path, or kind and Tableau LUID | Local / all | Yes | No | No | Collision/path-boundary guard; no remote move | Update | Local filesystem move plus metadata validation | A1 §§7.5, 8.1 and V1 exclusions; C1 §2.1 | Architecture-locked local contract |
| workspace.artifact.delete | `tadx workspace artifact delete` | Delete one exact managed local artifact, or preview the operation. | Change | Ship | CLI | - | Logical workspace name plus kind and LUID, or exact workspace-relative managed artifact path | Local / all | Yes | No | Yes | Exact identity; workspace boundary; dirty state explicit; never expands into generic cleanup | Delete | Local filesystem removal plus workspace metadata update | Maintainer decision; A1 §§6.1, 7.5–7.6, 8.1–8.2 | Architecture-locked local boundary; detailed cleanup policy remains deferred |
| workspace.clean | `tadx workspace clean` | Remove explicitly selected disposable local state while preserving canonical artifacts by default. | Change | Ship | CLI | - | Workspace and cleanup class | Local / all | Yes | No | No | Path-boundary guard; managed payloads preserved by default | None | Local `.tadx/` and temporary state | A1 §§5.8, 7.5–7.6, 9.2; C1 §2.1 | Architecture-locked local contract |
| workspace.set-default | `tadx workspace set-default` | Set one registered and available workspace as the general default. | Change | Ship | CLI | - | Exact logical workspace name | Local / all | Yes | No | No | Workspace must exist, remain registered, and have a valid manifest | None | Local workspace registry configuration | Workspace architecture and local manager tests | Local contract verified by manager and CLI tests |
| workspace.unregister | `tadx workspace unregister` | Remove one workspace registration while preserving every file in its root. | Change | Ship | CLI | - | Exact logical workspace name | Local / all | Yes | No | No | Exact registration; default-reference guard; filesystem remains untouched | None | Local workspace registry configuration | Workspace architecture and local manager tests | Local contract verified by manager and CLI tests |
| workspace.delete | `tadx workspace delete` | Remove one exact registered workspace and its managed root, or preview the operation. | Change | Ship | CLI | - | Exact logical workspace name | Local / all | Yes | No | Yes | Registered root, canonical containment, dirty-state, manifest, symlink, and broad-target guards; default-reference guard | Delete | Local workspace registry and managed root | Workspace architecture and local manager tests | Local contract verified by manager and CLI tests |
| search.run | `tadx search [term]` | Search supported content, administration, and Pulse resources. | Find | Ship | CLI | `search-content` | Optional text; broad or concrete `--type`; environment; optional `--catalog` | Cloud / Server; Pulse types require Tableau Cloud | No | No | No | Live Tableau by default; `--catalog` is local-only; bounded output; blank text requires a concrete type and uses list semantics | None | Existing bounded resource list adapters or local normalized catalog index | Architecture decision; existing list and catalog contracts | Architecture-locked shared search contract with hermetic action and adapter tests |
| doctor.run | `tadx doctor` | Diagnose config, PAT presence and validity, Tableau connectivity, catalog, workspace, and logging context without mutation. | Inspect | Ship | CLI | - | Optional environment/workspace scopes | Cloud / Server / Pulse as configured | No | No | No | Redacted checks; never probes or reports Tableau MCP connectivity; no persistent logging unless `TADX_LOG_LEVEL` is set | None | Local validators plus read-only Tableau auth and connectivity probes | A1 §§1.5, 5.12, 7.7, 9.1; maintainer MCP ownership decision | Architecture-locked; the user and host agent own Tableau MCP connection state |

### 2.2 Workbook, datasource, flow, lineage, and project lifecycle

| Capability ID | Public command or delegated surface | Resource and user outcome | Type | V1 status | Owner | MCP overlap | Selectors | Products / availability | Local write | Remote mutation | Supports `--preview` | Safety / guard | Artifact effect | Upstream operation | Evidence | Validation / blocker |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| workbook.list | `tadx content workbook list` | List workbooks with bounded lifecycle metadata. | Find | Ship | CLI | `list-workbooks` | Environment/site; project/owner/filter | Cloud / Server | No | No | No | Live Tableau by default; `--catalog` is local-only; no fallback; bounded continuation | None | `GET /api/{version}/sites/{site-id}/workbooks` | `docs/evidence/group2-inventory-rest-contract.md`; hermetic catalog source-selection tests | Contract-verified REST inventory read, bounded continuation, and explicit source selection |
| workbook.inspect | `tadx content workbook inspect` | Inspect one authoritative workbook and lifecycle metadata. | Inspect | Ship | CLI | `get-workbook` | Workbook LUID or exact name/project path | Cloud / Server | No | No | No | Live Tableau by default; `--catalog` is local-only; no fallback; exact resolution; ambiguity fails | None | `GET /api/{version}/sites/{site-id}/workbooks/{workbook-id}` | `docs/evidence/group2-inventory-rest-contract.md`; hermetic catalog source-selection tests | Contract-verified exact REST read, canonical project selection, and explicit source selection |
| workbook.pull | `tadx content workbook pull` | Download one workbook, capture bounded lineage, and optionally acquire its direct published datasource dependencies. | Deliver | Ship | CLI | `download-workbook` | Workbook LUID/exact path; logical workspace; optional `--include-pds` | Cloud / Server | Yes | No | No | Dirty workbook re-pull requires `--overwrite`; dirty dependencies stop acquisition; incomplete lineage warns; bundle persistence is recoverable | Create / update workbook, lineage sidecar, and optional datasource siblings | Workbook content GET; Metadata GraphQL POST; datasource exact/content GET | A1 §§6.3–6.4, 8.2, 8.6; C1 §§2.3, 5.4, 5.13; official REST and Metadata captures; Tableau Cloud happy-path capture | Contract-verified; published-datasource traversal captured live; lineage schema and hermetic tests recorded in `docs/evidence/lineage-metadata-contract.md`; remaining exact sources and tests recorded in `docs/evidence/phase1-rest-contract.md`; artifact contract architecture-locked |
| workbook.publish | `tadx content workbook publish` | Publish one local workbook to an explicit target, or preview the operation. | Deliver | Ship | CLI | - | Logical workspace plus exact workspace-relative managed workbook directory; target defaults to artifact source, explicit environment/site/project overrides; optional exact existing workbook | Cloud / Server; TWB validation API only on API 3.29 / Tableau 2026.2+ per C1 | No | Yes | Yes | Preview shows resolved environment/name/LUID; collision/overwrite explicit; no fuzzy target | Read / publish | `POST /api/{version}/sites/{site-id}/workbooks`; upload sessions; optional internal `validateWorkbook` for TWB | A1 §§5.11, 6.5–6.8, 8.6; C1 §§2.3, 5.4; S1 workbook-check correction; local official REST capture | Contract-verified; exact source and tests recorded in `docs/evidence/phase1-rest-contract.md`; standalone `workbook check` not admitted |
| workbook.delete | `tadx content workbook delete` | Delete one exact remote workbook, or preview the operation. | Change | Ship | CLI | - | Workbook LUID or exact name/project path; explicit environment/site | Cloud / Server | No | Yes | Yes | Re-resolve authoritative LUID immediately before delete; no cascade claims; repeated missing target is explicit | Delete | `DELETE /api/{version}/sites/{site-id}/workbooks/{workbook-id}` | `docs/evidence/phase1-rest-contract.md` | Contract-verified exact DELETE with authoritative LUID revalidation |
| workbook.move | `tadx content workbook move` | Move one exact workbook to one exact project on the same site, or preview the operation. | Change | Ship | CLI | - | Workbook LUID or exact name/project path; exact destination project LUID/path; explicit environment/site | Cloud / Server | No | Yes | Yes | Re-resolve source and destination immediately before mutation; equal destination is an exit-zero no-op; no cross-site move | None | `PUT /api/{version}/sites/{site-id}/workbooks/{workbook-id}` with destination project | `docs/evidence/content-update-rest-contract.md` | Contract-verified exact same-site movement, preview, no-op, drift, and uncertain outcome |
| workbook.update | `tadx content workbook update` | Rename one exact workbook or replace its owner, or preview the operation. | Change | Ship | CLI | - | Workbook LUID or exact name/project path; explicit new name and/or owner LUID; explicit environment/site | Cloud / Server | No | Yes | Yes | Re-resolve authoritative identity immediately before mutation; at least one change required; equal values are an exit-zero no-op | None | `PUT /api/{version}/sites/{site-id}/workbooks/{workbook-id}` | `docs/evidence/content-update-rest-contract.md` | Contract-verified bounded rename and owner replacement with preview, no-op, drift, and uncertain outcome |
| datasource.list | `tadx content datasource list` | List published datasources with bounded lifecycle metadata. | Find | Ship | CLI | `list-datasources` | Environment/site; project/owner/filter | Cloud / Server | No | No | No | Live Tableau by default; `--catalog` is local-only; no fallback; bounded continuation | None | `GET /api/{version}/sites/{site-id}/datasources` | `docs/evidence/group2-inventory-rest-contract.md`; hermetic catalog source-selection tests | Contract-verified REST inventory read, bounded continuation, and explicit source selection |
| datasource.inspect | `tadx content datasource inspect` | Inspect one datasource, with bounded field/model/composition detail when requested. | Inspect | Ship | CLI | `list-datasources`; `get-datasource-metadata` | Datasource LUID or exact name/project path; bounded detail options | Cloud / Server; VDS metadata/model Server 2025.1+ per C1 | No | No | No | Live Tableau by default; `--catalog` is local-only; no fallback; record Metadata API permission mode and partial warnings | None | `GET .../datasources/{id}`; optional Metadata API and VDS detail | `docs/evidence/group2-inventory-rest-contract.md`; hermetic catalog source-selection tests | Contract-verified base REST identity, lifecycle read, and explicit source selection; Metadata, VDS, and composition detail remain gated |
| datasource.schema | `tadx content datasource schema` | Inspect one datasource's logical tables and search a bounded field projection. | Inspect | Ship | CLI | `get-datasource-metadata` | Authoritative datasource LUID; optional field text, role, table, and raw field ID filters | Cloud / Server with VDS or Metadata API access | No | No | No | Query Tableau by default; `--catalog` is local-only with no source fallback; preserve raw field identity separately from display captions | None | VDS `read-metadata`, VDS `describe-datasource`, then Metadata API fallback | Proven existing field-catalog implementation plus hermetic TADX contract tests | Contract-verified VDS-first field discovery with bounded output and Metadata API fallback |
| datasource.pull | `tadx content datasource pull` | Download one datasource while preserving its native package and composition provenance, and capture bounded lineage. | Deliver | Ship | CLI | - | Datasource LUID/exact path; logical workspace | Cloud / Server; composed round-trip requires Tableau 2026.2 behavior per C1 | Yes | No | No | Dirty re-pull requires `--overwrite`; incomplete lineage warns; no package-semantic loss | Create / update package and lineage sidecar | `GET /api/{version}/sites/{site-id}/datasources/{datasource-id}/content` plus focused Metadata GraphQL reads | `docs/evidence/datasource-lifecycle-rest-contract.md`; `docs/evidence/lineage-metadata-contract.md` | Contract-verified native package preservation and bounded lineage; composition authoring remains blocked |
| datasource.publish | `tadx content datasource publish` | Publish one local datasource, including explicit immediate-parent references for composed artifacts, or preview the operation. | Deliver | Ship | CLI | - | Logical workspace plus exact workspace-relative managed datasource directory; target defaults to artifact source, explicit environment/site/project overrides; optional exact existing datasource; immediate parents for composed artifacts | Cloud / Server; composed path API 3.29 / Tableau 2026.2 per C1 | No | Yes | Yes | Overwrite/append/replace never inferred; preview shows resolved environment/name/LUID; stop on terminal failure | Read / publish | `POST /api/{version}/sites/{site-id}/datasources`; upload sessions; `parentDataSourceUrls` for composed path | `docs/evidence/datasource-lifecycle-rest-contract.md` | Contract-verified ordinary and composed package publication; composition authoring remains deferred |
| datasource.delete | `tadx content datasource delete` | Delete one exact remote datasource, or preview the operation. | Change | Ship | CLI | - | Datasource LUID or exact name/project path; explicit environment/site | Cloud / Server | No | Yes | Yes | Re-resolve authoritative LUID immediately before delete; preserve dependency failures; no cascade claims | Delete | `DELETE /api/{version}/sites/{site-id}/datasources/{datasource-id}` | `docs/evidence/datasource-lifecycle-rest-contract.md` | Contract-verified exact DELETE with authoritative LUID revalidation |
| datasource.move | `tadx content datasource move` | Move one exact published datasource to one exact project on the same site, or preview the operation. | Change | Ship | CLI | - | Datasource LUID or exact name/project path; exact destination project LUID/path; explicit environment/site | Cloud / Server | No | Yes | Yes | Re-resolve source and destination immediately before mutation; equal destination is an exit-zero no-op; no cross-site move | None | `PUT /api/{version}/sites/{site-id}/datasources/{datasource-id}` with destination project | `docs/evidence/content-update-rest-contract.md` | Contract-verified exact same-site movement, preview, no-op, drift, and uncertain outcome |
| datasource.update | `tadx content datasource update` | Rename one exact published datasource or replace its owner, or preview the operation. | Change | Ship | CLI | - | Datasource LUID or exact name/project path; explicit new name and/or owner LUID; explicit environment/site | Cloud / Server | No | Yes | Yes | Re-resolve authoritative identity immediately before mutation; at least one change required; equal values are an exit-zero no-op | None | `PUT /api/{version}/sites/{site-id}/datasources/{datasource-id}` | `docs/evidence/content-update-rest-contract.md` | Contract-verified bounded rename and owner replacement with preview, no-op, drift, and uncertain outcome |
| flow.list | `tadx content flow list` | List flows with bounded lifecycle metadata. | Find | Ship | CLI | `list-flows` | Environment/site; project/owner/filter | Cloud / Server with flow support; REST API 3.3+ per C1 | No | No | No | Live Tableau by default; `--catalog` is local-only; no fallback; bounded continuation | None | `GET /api/{version}/sites/{site-id}/flows` | Official REST capture in `docs/evidence/flow-rest-contract.md`; hermetic API and catalog source-selection tests | Contract-verified bounded inventory and explicit source selection; live deployment verification is not claimed |
| flow.inspect | `tadx content flow inspect` | Inspect one authoritative flow and its direct lifecycle metadata. | Inspect | Ship | CLI | `get-flow` | Flow LUID or exact name/project path | Cloud / Server with flow support | No | No | No | Live Tableau by default; `--catalog` is local-only; no fallback; exact resolution; ambiguity fails | None | `GET /api/{version}/sites/{site-id}/flows/{flow-id}` | Official REST capture in `docs/evidence/flow-rest-contract.md`; hermetic API and catalog source-selection tests | Contract-verified exact read and explicit source selection; live deployment verification is not claimed |
| flow.pull | `tadx content flow pull` | Download one flow unchanged, capture bounded lineage, and create a provenance-bearing local artifact. | Deliver | Ship | CLI | `get-flow` is metadata-only overlap | Flow LUID or exact name/project path; logical workspace | Cloud / Server with flow support | Yes | No | No | Dirty re-pull requires `--overwrite`; incomplete lineage warns | Create / update package and lineage sidecar | Flow content GET plus focused Metadata GraphQL reads | Official REST and Metadata captures plus hermetic contract tests in `docs/evidence/flow-rest-contract.md` and `docs/evidence/lineage-metadata-contract.md` | Contract-verified; live deployment verification is not claimed; artifact contract architecture-locked |
| flow.publish | `tadx content flow publish` | Publish one local TFL/TFLX to its source target or an explicit target, or preview the operation. | Deliver | Ship | CLI | - | Logical workspace plus exact workspace-relative managed flow directory; target defaults to artifact source, explicit environment/site/project overrides; optional exact existing flow | Cloud / Server with flow support | No | Yes | Yes | Preview shows resolved environment/name/LUID; overwrite explicit | Read / publish | `POST /api/{version}/sites/{site-id}/flows`; upload sessions for large files | Official REST capture and hermetic contract tests in `docs/evidence/flow-rest-contract.md` | Contract-verified; live deployment verification is not claimed |
| flow.move | `tadx content flow move` | Move one exact flow to one exact project on the same site, or preview the operation. | Change | Ship | CLI | - | Flow LUID or exact name/project path; exact destination project LUID/path; explicit environment/site | Cloud / Server with flow support | No | Yes | Yes | Source and destination re-resolved; cross-site move rejected; no connection or schedule rewrite | None | `PUT /api/{version}/sites/{site-id}/flows/{flow-id}` with an exact destination project | Official REST capture and hermetic contract tests in `docs/evidence/flow-rest-contract.md` | Contract-verified; live deployment verification is not claimed |
| flow.update | `tadx content flow update` | Replace the owner of one exact flow, or preview the operation. | Change | Ship | CLI | - | Flow LUID or exact name/project path; explicit owner LUID; explicit environment/site | Cloud / Server with flow support; owner endpoint requires API 3.27+ | No | Yes | Yes | Re-resolve authoritative identity immediately before mutation; equal owner is an exit-zero no-op; Tableau does not expose a flow rename field | None | `PUT /api/{version}/sites/{site-id}/flows/{flow-id}/owner/{user-id}` | `docs/evidence/content-update-rest-contract.md` | Contract-verified owner replacement with preview, no-op, drift, and uncertain outcome; flow rename is excluded |
| flow.delete | `tadx content flow delete` | Delete one exact remote flow, or preview the operation. | Change | Ship | CLI | - | Flow LUID or exact name/project path; explicit environment/site | Cloud / Server with flow support | No | Yes | Yes | Re-resolve authoritative LUID immediately before delete; preserve dependency failures; no cascade claims | Delete | `DELETE /api/{version}/sites/{site-id}/flows/{flow-id}` | Official REST capture and hermetic contract tests in `docs/evidence/flow-rest-contract.md` | Contract-verified; live deployment verification is not claimed |
| lineage.pull | `tadx content lineage pull` | Capture bounded lineage for one exact workbook, published datasource, or flow without downloading its native package. | Deliver | Ship | CLI | Metadata reads overlap with resource metadata tools | Resource kind plus REST LUID or exact name/project path; logical workspace; bounded direction and depth | Cloud / Server with Metadata API | Yes | No | No | REST and Metadata identities remain distinct; incomplete results are explicit; no dependency acquisition | Create metadata-only lineage artifact | Focused Metadata GraphQL reads plus authoritative REST identity resolution | Official Metadata guide and schema captures plus hermetic contract tests in `docs/evidence/lineage-metadata-contract.md` | Contract-verified; live deployment verification is not claimed |
| project.list | `tadx content project list` | List projects and their authoritative parent identity. | Find | Ship | CLI | `list-projects` | Environment/site; filters | Cloud / Server | No | No | No | Live Tableau by default; `--catalog` is local-only; no fallback; bounded continuation | None | `GET /api/{version}/sites/{site-id}/projects` | Official REST capture in `docs/evidence/project-rest-contract.md`; hermetic API and catalog source-selection tests | Contract-verified bounded inventory and explicit source selection; live deployment verification is not claimed |
| project.inspect | `tadx content project inspect` | Resolve and inspect one exact shallow project context. | Inspect | Ship | CLI | `list-projects` | Project LUID or exact slash-delimited path | Cloud / Server | No | No | No | Live Tableau by default; `--catalog` is local-only; no fallback; ambiguous path fails | None | Exact resolution from authoritative project pages | Official REST capture in `docs/evidence/project-rest-contract.md`; hermetic API and catalog source-selection tests | Contract-verified exact path resolution and explicit source selection; live deployment verification is not claimed |
| project.create | `tadx content project create` | Create one project, optionally under an explicit parent, or preview the operation. | Change | Ship | CLI | - | Explicit environment/site; optional parent LUID/path | Cloud / Server | No | Yes | Yes | Same-name collision and ambiguous parent fail | None | `POST /api/{version}/sites/{site-id}/projects` | `docs/evidence/project-rest-contract.md` | Contract-verified exact create, parent, collision, and response identity behavior |
| project.update | `tadx content project update` | Update bounded project metadata without changing its parent, or preview the operation. | Change | Ship | CLI | - | Project LUID/exact path | Cloud / Server | No | Yes | Yes | Equal values are an exit-zero no-op; hierarchy changes use `project.move` | None | `PUT /api/{version}/sites/{site-id}/projects/{project-id}` | `docs/evidence/project-rest-contract.md` | Contract-verified bounded metadata update and authoritative no-op behavior |
| project.move | `tadx content project move` | Reparent one exact project under one exact parent on the same site, or preview the operation. | Change | Ship | CLI | - | Project LUID/exact path; exact destination parent LUID/path; explicit environment/site | Cloud / Server | No | Yes | Yes | Re-resolve project and parent immediately before mutation; equal parent is an exit-zero no-op; prevent self-parent and descendant cycles | None | `PUT /api/{version}/sites/{site-id}/projects/{project-id}` with parent project | `docs/evidence/content-update-rest-contract.md` | Contract-verified exact reparenting, preview, no-op, cycle guards, drift, and uncertain outcome |
| project.delete | `tadx content project delete` | Delete one exact project, or preview the operation. | Change | Ship | CLI | - | Authoritative project LUID | Cloud / Server | No | Yes | Yes | Exact LUID revalidated; preview identifies the target; upstream cascade behavior is explicit | None | `DELETE /api/{version}/sites/{site-id}/projects/{project-id}` | `docs/evidence/project-rest-contract.md` | Contract-verified exact deletion, preview, identity revalidation, and uncertain-outcome handling |

### 2.3 Pulse lifecycle

| Capability ID | Public command or delegated surface | Resource and user outcome | Type | V1 status | Owner | MCP overlap | Selectors | Products / availability | Local write | Remote mutation | Supports `--preview` | Safety / guard | Artifact effect | Upstream operation | Evidence | Validation / blocker |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| pulse.definition.list | `tadx pulse definition list` | List Pulse metric definitions with bounded token continuation. | Find | Ship | CLI | `list-all-pulse-metric-definitions`; `list-pulse-metric-definitions-from-definition-ids` | Site; optional bounded cursor | Tableau Cloud / Pulse only | No | No | No | Bounded page token output; `--catalog` never contacts Tableau or falls back | None | `GET /api/-/pulse/definitions` | `docs/evidence/pulse-live-contract.md`; hermetic TADX client and action tests | Live-verified bounded list envelope and continuation on Tableau Cloud |
| pulse.definition.inspect | `tadx pulse definition inspect` | Inspect one complete Pulse definition and configuration. | Inspect | Ship | CLI | `list-pulse-metric-definitions-from-definition-ids` | Exact definition LUID | Tableau Cloud / Pulse only | No | No | No | Exact LUID; `--catalog` never contacts Tableau or falls back | None | `GET /api/-/pulse/definitions/{definition_id}` | `docs/evidence/pulse-live-contract.md`; hermetic TADX client and action tests | Live-verified exact identity and preserved configuration on Tableau Cloud |
| pulse.definition.pull | `tadx pulse definition pull` | Materialize one definition as a JSON-backed artifact with provenance and baseline. | Deliver | Ship | CLI | Read overlap only | Exact definition LUID; logical workspace | Tableau Cloud / Pulse only | Yes | No | No | Dirty re-pull requires `--overwrite`; remote read is authoritative | Create / update | Definition GET plus local artifact manager | `docs/evidence/pulse-live-contract.md`; hermetic TADX action and artifact tests | Live-verified exact read and recoverable artifact write on Tableau Cloud |
| pulse.definition.create | `tadx pulse definition create` | Create one definition plus its Tableau-created default metric from bounded intent, or preview the operation. | Change | Ship | CLI | - | Explicit site; datasource LUID; exact raw measure/date field IDs; bounded configuration | Tableau Cloud / Pulse only | No | Yes | Yes | Validate fields live; exact name/datasource collision; revalidate before mutation; unknown outcomes preserve created identity | Read / publish | `POST /api/-/pulse/definitions`; bounded default-metric polling | `docs/evidence/pulse-live-contract.md`; proven Pulse payload and hermetic TADX action and client tests | Live-verified creation and bounded default-metric resolution for a Pulse-eligible datasource on Tableau Cloud |
| pulse.definition.delete | `tadx pulse definition delete` | Delete one exact definition, or preview the operation. | Change | Ship | CLI | - | Definition ID | Tableau Cloud / Pulse only | No | Yes | Yes | Exact GET before deletion; Tableau remains authoritative for dependency and cascade behavior | None | `DELETE /api/-/pulse/definitions/{definition_id}` | `docs/evidence/pulse-live-contract.md`; hermetic TADX action and client tests | Live-verified exact deletion with HTTP 204 on Tableau Cloud |
| pulse.metric.list | `tadx pulse metric list` | List metrics in one definition with bounded continuation. | Find | Ship | CLI | `list-pulse-metrics-from-metric-definition-id` | Exact definition LUID; optional bounded cursor | Tableau Cloud / Pulse only | No | No | No | Definition-scoped identity; `--catalog` never contacts Tableau or falls back | None | `GET /api/-/pulse/definitions/{definition_id}/metrics` | `docs/evidence/pulse-live-contract.md`; hermetic TADX client and action tests | Live-verified definition-scoped metric inventory on Tableau Cloud |
| pulse.metric.inspect | `tadx pulse metric inspect` | Inspect one exact Pulse metric specification. | Inspect | Ship | CLI | `list-pulse-metrics-from-metric-ids` | Exact metric LUID | Tableau Cloud / Pulse only | No | No | No | Exact LUID; preserve unrecognized specification fields; `--catalog` never contacts Tableau or falls back | None | `GET /api/-/pulse/metrics/{metric_id}` | `docs/evidence/pulse-live-contract.md`; hermetic TADX client and action tests | Live-verified exact metric identity and specification preservation on Tableau Cloud |
| pulse.metric.fork | `tadx pulse metric fork` | Derive one metric by changing bounded timeframe or dimension filters, or preview the operation. | Change | Ship | CLI | - | Exact source metric LUID; timeframe or allowed-dimension filters | Tableau Cloud / Pulse only | No | Yes | Yes | Preserve source specification; require a meaningful change; use get-or-create; reconcile metric, definition, datasource, and site ownership | None | `POST /api/-/pulse/metrics:getOrCreate` plus bounded exact-read reconciliation | `docs/evidence/pulse-live-contract.md`; hermetic TADX action and client tests | Live-verified fork creation and ownership reconciliation on Tableau Cloud |
| pulse.metric.delete | `tadx pulse metric delete` | Delete one exact metric, or preview the operation. | Change | Ship | CLI | - | Metric ID | Tableau Cloud / Pulse only | No | Yes | Yes | Exact GET before deletion; Tableau remains authoritative for dependency behavior | None | `DELETE /api/-/pulse/metrics/{metric_id}` | `docs/evidence/pulse-live-contract.md`; hermetic TADX action and client tests | Live-verified exact deletion with HTTP 204 on Tableau Cloud |
| pulse.metric.followers | `tadx pulse metric followers` | List exact user and group subscriptions for one metric. | Find | Ship | CLI | `list-pulse-metric-subscriptions` | Exact metric LUID | Tableau Cloud / Pulse only | No | No | No | Bound normalized output; reject incomplete or mismatched subscription identity; `--catalog` never contacts Tableau or falls back | None | `GET /api/-/pulse/subscriptions?metric_id={metric_id}` | `docs/evidence/pulse-live-contract.md`; hermetic TADX action and client tests | Live-verified exact subscription inventory on Tableau Cloud |
| pulse.metric.follow | `tadx pulse metric follow` | Converge one exact user or group metric subscription, or preview the operation. | Change | Ship | CLI | `list-pulse-metric-subscriptions` for resolution | Exact metric LUID plus exactly one user or group LUID | Tableau Cloud / Pulse only | No | Yes | Yes | No ambiguous subscriber; a proven duplicate response converges to already-following | None | `POST /api/-/pulse/subscriptions:batchCreate` | `docs/evidence/pulse-live-contract.md`; hermetic TADX action and client tests | Live-verified user subscription creation on Tableau Cloud |
| pulse.metric.unfollow | `tadx pulse metric unfollow` | Remove one exact metric subscription, or preview the operation. | Change | Ship | CLI | `list-pulse-metric-subscriptions` for resolution | Exact subscription LUID, or metric plus exact user/group resolving to one subscription | Tableau Cloud / Pulse only | No | Yes | Yes | Ambiguity fails; relationship selectors revalidate to the same subscription before mutation | None | `DELETE /api/-/pulse/subscriptions/{subscription_id}` | `docs/evidence/pulse-live-contract.md`; hermetic TADX action and client tests | Live-verified exact subscription deletion with HTTP 204 on Tableau Cloud |

### 2.4 Narrow administration

| Capability ID | Public command or delegated surface | Resource and user outcome | Type | V1 status | Owner | MCP overlap | Selectors | Products / availability | Local write | Remote mutation | Supports `--preview` | Safety / guard | Artifact effect | Upstream operation | Evidence | Validation / blocker |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| admin.user.list | `tadx admin user list` | List site users with bounded administration metadata. | Find | Ship | CLI | `list-users` | Environment/site; filters | Cloud / Server | No | No | No | Live Tableau by default; `--catalog` is local-only; no fallback; bounded continuation; secret-free | None | `GET /api/{version}/sites/{site-id}/users` | `docs/evidence/admin-rest-contract.md`; hermetic catalog source-selection tests | Contract-verified bounded user pagination and explicit source selection |
| admin.user.inspect | `tadx admin user inspect` | Inspect one exact site user. | Inspect | Ship | CLI | `list-users` | User LUID or exact username/email where supported | Cloud / Server | No | No | No | Live Tableau by default; `--catalog` is local-only; no fallback; ambiguity fails | None | `GET /api/{version}/sites/{site-id}/users/{user-id}` | `docs/evidence/admin-rest-contract.md`; hermetic catalog source-selection tests | Contract-verified exact user identity resolution and explicit source selection |
| admin.user.create | `tadx admin user create` | Add one user to a site with explicit role/auth settings, or preview the operation. | Change | Ship | CLI | - | Explicit site; username | Cloud / Server; fields vary by product/version | No | Yes | Yes | No inferred role/auth setting; quota/license failures preserved | None | `POST /api/{version}/sites/{site-id}/users` | `docs/evidence/admin-rest-contract.md` | Contract-verified explicit create and optional preview behavior |
| admin.user.update | `tadx admin user update` | Update supported attributes of one exact user, or preview the operation. | Change | Ship | CLI | `update-user` | User LUID | Cloud / Server; fields vary | No | Yes | Yes | Equal values no-op when authoritative pre-read exists | None | `PUT /api/{version}/sites/{site-id}/users/{user-id}` | `docs/evidence/admin-rest-contract.md` | Contract-verified bounded user update and no-op behavior |
| admin.user.delete | `tadx admin user delete` | Remove one exact user from a site without hidden ownership reassignment, or preview the operation. | Change | Ship | CLI | - | User LUID | Cloud / Server | No | Yes | Yes | Ownership constraints surfaced; never silently transfer content | None | `DELETE /api/{version}/sites/{site-id}/users/{user-id}` | `docs/evidence/admin-rest-contract.md` | Contract-verified exact delete without ownership reassignment |
| admin.group.list | `tadx admin group list` | List groups with bounded identity and directory metadata. | Find | Ship | CLI | - | Environment/site; filters | Cloud / Server | No | No | No | Live Tableau by default; `--catalog` is local-only; no fallback; bounded continuation | None | `GET /api/{version}/sites/{site-id}/groups` | `docs/evidence/admin-rest-contract.md`; hermetic catalog source-selection tests | Contract-verified bounded group pagination and explicit source selection |
| admin.group.inspect | `tadx admin group inspect` | Inspect one exact group and, when requested, its direct membership. | Inspect | Ship | CLI | - | Group LUID or exact name | Cloud / Server | No | No | No | Live Tableau by default; `--catalog` is local-only; no fallback; exact group; membership pages normalized | None | `GET .../groups`; `GET .../groups/{group-id}/users` | `docs/evidence/admin-rest-contract.md`; hermetic catalog source-selection tests | Contract-verified exact group, bounded direct membership, and explicit source selection |
| admin.group.create | `tadx admin group create` | Create one site group with explicit supported settings, or preview the operation. | Change | Ship | CLI | - | Explicit site; group name | Cloud / Server; directory/import fields vary | No | Yes | Yes | Collision and directory-setting failures preserved | None | `POST /api/{version}/sites/{site-id}/groups` | `docs/evidence/admin-rest-contract.md` | Contract-verified explicit create and collision behavior |
| admin.group.update | `tadx admin group update` | Update group attributes or converge direct membership, or preview the operation. | Change | Ship | CLI | - | Group LUID; exact user LUIDs | Cloud / Server | No | Yes | Yes | Full membership diff preview; ordered calls; stop on first failure; no rollback/resume | None | `PUT .../groups/{group-id}`; member pre-read; `POST .../groups/{group-id}/users`; `DELETE .../users/{user-id}` | `docs/evidence/admin-rest-contract.md` | Contract-verified metadata and deterministic membership convergence |
| admin.group.member.add | `tadx admin group member add` | Add one exact user to one exact group without replacing other members, or preview the operation. | Change | Ship | CLI | - | Exact group LUID and user LUID; explicit environment/site | Cloud / Server | No | Yes | Yes | Authoritative membership pre-read; existing membership is an exit-zero no-op; preserve unrelated members | None | `GET .../groups/{group-id}/users`; `POST .../groups/{group-id}/users` | `docs/evidence/admin-rest-contract.md` | Contract-verified exact incremental add, preview, no-op, revalidation, and uncertain outcome |
| admin.group.member.remove | `tadx admin group member remove` | Remove one exact user from one exact group without replacing other members, or preview the operation. | Change | Ship | CLI | - | Exact group LUID and user LUID; explicit environment/site | Cloud / Server | No | Yes | Yes | Authoritative membership pre-read; absent membership is an exit-zero no-op; preserve unrelated members | None | `GET .../groups/{group-id}/users`; `DELETE .../groups/{group-id}/users/{user-id}` | `docs/evidence/admin-rest-contract.md` | Contract-verified exact incremental removal, preview, no-op, revalidation, and uncertain outcome |
| admin.group.delete | `tadx admin group delete` | Delete one exact group without deleting its users, or preview the operation. | Change | Ship | CLI | - | Group LUID | Cloud / Server | No | Yes | Yes | Known direct facts only; no claim of full permission-impact analysis | None | `DELETE /api/{version}/sites/{site-id}/groups/{group-id}` | `docs/evidence/admin-rest-contract.md` | Contract-verified exact group deletion without user deletion |
| admin.permission.inspect | `tadx admin permission inspect` | Inspect explicit/default permission rules for one supported resource. | Inspect | Ship | CLI | - | Resource kind plus exact LUID; optional principal/capability filters | Cloud / Server | No | No | No | Distinguish direct/default/inherited/unknown; no effective-permission engine | None | Workbook/datasource/flow/project permission GET endpoints | `docs/evidence/admin-rest-contract.md` | Contract-verified normalized permission source facts; no effective-permission claim |
| admin.permission.create | `tadx admin permission create` | Add one explicit permission capability and mode for an exact principal, or preview the operation. | Change | Ship | CLI | - | Resource kind/LUID, principal type/LUID, capability, mode; optional project default kind | Cloud / Server | No | Yes | Yes | Direct or project-default rules only; inherited and conflicting opposite modes fail; identical rules are no-op | None | Permission PUT endpoints for workbook, datasource, flow, project, and project defaults | `docs/evidence/permission-mutations-rest-contract.md` | Contract-verified exact rule creation, no-op, preview, revalidation, and conflict handling |
| admin.permission.delete | `tadx admin permission delete` | Delete one explicit permission capability and mode for an exact principal, or preview the operation. | Change | Ship | CLI | - | Resource kind/LUID, principal type/LUID, capability, mode; optional project default kind | Cloud / Server | No | Yes | Yes | Direct or project-default rules only; inherited and mismatched modes fail; absent rules are no-op | None | Permission DELETE endpoints for workbook, datasource, flow, project, and project defaults | `docs/evidence/permission-mutations-rest-contract.md` | Contract-verified exact rule deletion, no-op, preview, revalidation, and uncertain-outcome handling |

### 2.5 Delegated discoverable operations

| Capability ID | Public command or delegated surface | Resource and user outcome | Type | V1 status | Owner | MCP overlap | Selectors | Products / availability | Local write | Remote mutation | Supports `--preview` | Safety / guard | Artifact effect | Upstream operation | Evidence | Validation / blocker |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| datasource.metadata | Tableau MCP `get-datasource-metadata` | Retrieve analytical field and datasource metadata for reasoning and query construction. | Inspect | Delegated | MCP | `get-datasource-metadata` | Published datasource LUID | Cloud / Server with Tableau MCP availability | No | No | No | The host agent selects and connects Tableau MCP; TADX does not inspect that connection | None | Tableau MCP `get-datasource-metadata` | `docs/evidence/tableau-mcp-delegation.md` | Delegated; not executed or proxied by TADX |
| datasource.query | Tableau MCP `query-datasource` | Run analytical queries against datasource data. | Inspect | Delegated | MCP | `query-datasource` | Datasource and query identity | Cloud / Server with Tableau MCP availability | No | No | No | The host agent selects and connects Tableau MCP; TADX does not inspect that connection | None | Tableau MCP `query-datasource` | `docs/evidence/tableau-mcp-delegation.md` | Delegated; not executed or proxied by TADX |
| view.list | Tableau MCP `list-views` | List views for analytical read workflows. | Find | Delegated | MCP | `list-views` | Workbook, project, owner, and name filters | Cloud / Server with Tableau MCP availability | No | No | No | The host agent selects and connects Tableau MCP; TADX does not inspect that connection | None | Tableau MCP `list-views` | `docs/evidence/tableau-mcp-delegation.md` | Delegated; not executed or proxied by TADX |
| view.inspect | Tableau MCP `get-view` | Inspect one view before requesting rendered data or an image. | Inspect | Delegated | MCP | `get-view` | Exact view LUID | Cloud / Server with Tableau MCP availability | No | No | No | The host agent selects and connects Tableau MCP; TADX does not inspect that connection | None | Tableau MCP `get-view` | `docs/evidence/tableau-mcp-delegation.md` | Delegated; not executed or proxied by TADX |
| view.data | Tableau MCP `get-view-data` | Retrieve rendered view data for analysis. | Inspect | Delegated | MCP | `get-view-data` | Exact view LUID and bounded filters | Cloud / Server with Tableau MCP availability | No | No | No | The host agent selects and connects Tableau MCP; TADX does not inspect that connection | None | Tableau MCP `get-view-data` | `docs/evidence/tableau-mcp-delegation.md` | Delegated; not executed or proxied by TADX |
| view.image | Tableau MCP `get-view-image` | Retrieve a rendered view image. | Inspect | Delegated | MCP | `get-view-image` | Exact view LUID and bounded render options | Cloud / Server with Tableau MCP availability | No | No | No | The host agent selects and connects Tableau MCP; TADX does not inspect that connection | None | Tableau MCP `get-view-image` | `docs/evidence/tableau-mcp-delegation.md` | Delegated; not executed or proxied by TADX |
| custom-view.list | Tableau MCP `list-custom-views` | List saved custom views for analytical read workflows. | Find | Delegated | MCP | `list-custom-views` | Workbook, owner, and name filters | Cloud / Server with Tableau MCP availability | No | No | No | The host agent selects and connects Tableau MCP; TADX does not inspect that connection | None | Tableau MCP `list-custom-views` | `docs/evidence/tableau-mcp-delegation.md` | Delegated; not executed or proxied by TADX |
| custom-view.data | Tableau MCP `get-custom-view-data` | Retrieve rendered data from one custom view. | Inspect | Delegated | MCP | `get-custom-view-data` | Exact custom-view LUID and bounded filters | Cloud / Server with Tableau MCP availability | No | No | No | The host agent selects and connects Tableau MCP; TADX does not inspect that connection | None | Tableau MCP `get-custom-view-data` | `docs/evidence/tableau-mcp-delegation.md` | Delegated; not executed or proxied by TADX |
| custom-view.image | Tableau MCP `get-custom-view-image` | Retrieve a rendered image from one custom view. | Inspect | Delegated | MCP | `get-custom-view-image` | Exact custom-view LUID and bounded render options | Cloud / Server with Tableau MCP availability | No | No | No | The host agent selects and connects Tableau MCP; TADX does not inspect that connection | None | Tableau MCP `get-custom-view-image` | `docs/evidence/tableau-mcp-delegation.md` | Delegated; not executed or proxied by TADX |
| pulse.metric.insight-bundle | Tableau MCP `generate-pulse-metric-value-insight-bundle` | Retrieve current Pulse metric values and generated insights. | Inspect | Delegated | MCP | `generate-pulse-metric-value-insight-bundle` | Exact metric LUIDs | Tableau Cloud / Pulse with Tableau MCP availability | No | No | No | The host agent selects and connects Tableau MCP; TADX does not inspect that connection | None | Tableau MCP `generate-pulse-metric-value-insight-bundle` | `docs/evidence/tableau-mcp-delegation.md` | Delegated; not executed or proxied by TADX |
| pulse.insight.brief | Tableau MCP `generate-pulse-insight-brief` | Generate a Pulse insight brief from exact metrics. | Inspect | Delegated | MCP | `generate-pulse-insight-brief` | Exact metric LUIDs and brief context | Tableau Cloud / Pulse with Tableau MCP availability | No | No | No | The host agent selects and connects Tableau MCP; TADX does not inspect that connection | None | Tableau MCP `generate-pulse-insight-brief` | `docs/evidence/tableau-mcp-delegation.md` | Delegated; not executed or proxied by TADX |
| workbook.author | Tableau Desktop / Desktop MCP | Create or semantically modify workbook content. | Change | Delegated | Tableau / Desktop MCP | Owning authoring surface | Workbook and authoring context | Tableau Desktop interoperability | N/A outside TADX | N/A outside TADX | N/A outside TADX | Owning surface controls safety | External artifact may become dirty | Tableau authoring surface | A1 §§1.6, 3.4, 8.6; C1 §§2.3, 3 | Delegated; TADX never transforms workbook XML |
| datasource.field-description.generate | Agent reasoning or an explicitly shipped skill; no TADX CLI command | Generate or revise field-description text using metadata and optional sampled statistics. | Change | Delegated | Agent / Skill | `get-datasource-metadata`; `query-datasource` as needed | Datasource and selected fields | Cloud / Server | Optional change set | No at reasoning stage | No at reasoning stage | Human or agent review before using the text outside TADX | Optional update | Procedure composed from MCP reads | A1 §§3.1, 4.4, 8.4, 11.4; C1 §2.4 | Delegated reasoning; remote field-description updates are deferred |

## 3. Shared execution invariants

### 3.1 Identity resolution

Tableau LUIDs are authoritative remote identity. Names, project paths, local paths, and catalog hits are selectors only.

1. Prefer an explicit LUID.
2. Otherwise resolve an exact name and, where applicable, exact slash-delimited project path.
3. Zero matches and multiple matches are deterministic errors.
4. Do not use fuzzy matching, interactive selector prompts, or silent redirection after a remote rename/move.
5. A cached catalog result may help select a resource, but every remote mutation re-resolves the exact target against Tableau.
6. Metadata API IDs are not assumed to equal REST/vizportal LUIDs. Store proven mappings.

### 3.2 Local write versus remote mutation

`Local write` covers config, catalog, workspace, artifact pull, and local artifact editing.

`Remote mutation` covers consequential changes to Tableau content, Pulse configuration, users, or groups. Every such row:

- targets the artifact's recorded source environment, site, and project when the caller omits a target,
- requires an exact target project when an explicit `--environment` overrides artifact provenance,
- requires an explicit environment, site, and project when the artifact lacks complete source provenance,
- runs by default when mutation execution is enabled,
- performs no mutation when the caller supplies `--preview`,
- remains visible in command help and capability discovery,
- returns `mutation.disabled` before command execution unless `TADX_ENABLE_MUTATIONS=1`,
- is not authorized merely because it is discoverable or enabled,
- cannot use `--force` as a substitute for mutation authorization.

Authentication/session establishment is not treated as a consequential resource mutation.

### 3.3 Mutation and preview

A remote mutation follows this sequence:

```text
resolve exact target
-> validate admitted inputs
-> emit a deterministic preview and stop when --preview is set
-> revalidate the exact target
-> mutate by default
-> report authoritative outcome
```

The preview includes the environment/site, resource identity, destination, changed fields, collision/overwrite mode, and independently meaningful substeps.
When artifact provenance resolves the target, TADX shows the resolved environment, content name, and LUID.
Before mutation, TADX re-resolves that LUID and fails if it was renamed, moved, or deleted instead of targeting another resource.
A recorded source environment absent from local configuration is a deterministic error.
There is no second confirmation prompt and no production-only prompt.

TADX does not promise generic optimistic concurrency or remote-change detection. Tableau remains authoritative. There are no generic automatic retries; a capability may add only a bounded retry proven safe and deterministic.

### 3.4 Pagination normalization

Adapters preserve upstream pagination while presenting one bounded public continuation envelope.

| Upstream family | Adapter rule |
| --- | --- |
| Classic REST lists | Preserve `pageNumber` / `pageSize` semantics and endpoint-specific limits. |
| Content search | Preserve `page` / `limit` and returned next/previous/page metadata. |
| Pulse | Preserve page-token semantics and do not assume all Pulse endpoints use an identical token shape. |
| Metadata API | Prefer connection cursors for large lists; never combine `after` and `offset`; preserve the documented maximum page size and warning state. |
| Catalog refresh | May exhaust all admitted upstream pages internally because results go to cache, not model context. A failed page makes the new generation incomplete. |

Default CLI list/search output is bounded. `--full` expands permitted output but does not justify dumping an unbounded site inventory into model context.

### 3.5 Asynchronous polling

When a released Tableau operation returns a job and no user decision is needed mid-flight:

- TADX performs upload-session mechanics and bounded polling internally.
- It returns terminal success, terminal failure, or timeout.
- It includes Tableau request/job IDs where available.
- It does not make the agent repeatedly poll.
- It exposes a separate job handle only when completion cannot reasonably fit one invocation.

Polling does not create a generic retry policy.

### 3.6 Partial warnings and errors

Single-resource atomic operations succeed or fail.

A deterministic multi-resource operation may return partial outcomes only when substeps are independently meaningful:

- shallow project pull/publish,
- group membership convergence.

Such operations stop on the first failed substep, report exact completed and failed work, do not roll back completed remote changes, and do not provide automatic resume in V1.

Metadata API warning-bearing responses are not ordinary complete success. Backfill, linked-result, inheritance, page-size, node-limit, time-limit, permission, sort, and rate-limit conditions must be preserved. Catalog refresh must not mark a warning-incomplete generation complete.

Structured errors include, where available: stable string ID, operation, exact selector/resource identity, environment/site, summary, upstream status/code/detail, retryability, deterministic corrective action, validation details, and request/job ID. Exit codes remain `0` success/no-op, `1` operation/runtime failure, and `2` usage error.

### 3.7 Artifact and re-pull behavior

Every pulled artifact contains:

1. the canonical native payload or `resource.json`,
2. `metadata.json` with source provenance and authoritative Tableau ID,
3. required `view.md`,
4. `lineage.json` when bounded lineage capture returns graph data or an explicit incomplete result,
5. a baseline fingerprint covering managed canonical content.

Every pulled artifact records its source environment, site, project, content name, and Tableau LUID in provenance metadata.
That recorded origin is the default publish target.
An explicit `--environment` overrides the source and requires an exact destination project.
Artifacts without complete source provenance require an explicit target.
External tools can edit artifacts.

Workbook, datasource, and flow pulls capture direct bounded upstream and downstream lineage by default.
Lineage capture is best-effort and never hides an otherwise successful native artifact pull.
`metadata.json` records lineage status, completeness, counts, and a relative pointer to `lineage.json`.
Warnings identify partial or unavailable lineage.
The graph stores Metadata API IDs separately from REST LUIDs and never uses a Metadata API ID for a write.
Successful compact pull output omits lineage details.
`--full` can expose a bounded lineage summary and relative sidecar path, but the graph remains file-backed.
Partial or unavailable capture remains visible as a compact warning.

Standalone `tadx content lineage pull` creates the same metadata and lineage sidecar without a native package.
The standalone operation supports explicit bounded direction, depth, and transitive traversal.
It never downloads dependencies or rewrites content.
All persisted paths are workspace-relative and slash-delimited.

Re-pull behavior is identity-based:

```text
current fingerprint == baseline
    -> warn
    -> replace from remote
    -> update provenance and baseline

current fingerprint != baseline
    -> stop
    -> require --overwrite

--overwrite
    -> warn
    -> replace
    -> update provenance and baseline
```

There is no automatic revision history.
Pull is a local write.
Native workbook, datasource, and flow packages are preserved; TADX does not silently author workbook or flow semantics.

#### Published datasource references and portability

A workbook that references one or more published datasources is source-site-bound.
Tableau validates those references and rejects a cross-site publish when the target lacks the referenced datasource identity.
To keep this from being a silent surprise, the workbook artifact provenance carries a portability contract:

- `portability`: `portable` when the workbook is self-contained, `source-site-bound` when it directly references published datasources, or `unknown` when detection did not return a complete authoritative result.
An empty value is accepted only for artifacts created before explicit `unknown` state.
- `published_datasources[]`: each direct published-datasource reference, recorded by authoritative REST LUID with the datasource name and source site as labels.
When a dependency is acquired via `--include-pds`, its entry also records the sibling datasource artifact's local path.
- `dependencies_acquired`: whether the referenced published datasources were downloaded as sibling artifacts.

Acquired dependencies are first-class sibling datasource artifacts under `artifacts/datasource/`, deduplicated by (source site, datasource LUID); the workbook is never modified, dependencies are never published, and acquisition never recurses transitively.

Two behaviors are built and do not depend on any remote contract:

1. The schema above and its `view.md` rendering.
2. A publish-time preview warning: publishing a `source-site-bound` workbook to another environment or site warns that Tableau can reject the package because its references remain source-site-bound.
Republishing to the artifact's recorded source (the default target) never warns.

TADX sends the native workbook package unchanged when the caller does not select `--preview` and preserves Tableau's authoritative response.
TADX does not add a separate cross-site published-datasource rejection gate.

Detection populates `portability` and `published_datasources[]` on every pull, and `--include-pds` acquires direct published datasource dependencies without recursion.
The default `workbook pull` downloads the workbook unchanged regardless; detection only adds provenance and never alters the payload.
Detection requires the Metadata API workbook-to-published-datasource LUID traversal because the REST connections endpoint does not expose the referenced datasource's LUID.
The official contract, local contract tests, and redacted live Tableau Cloud request and response are recorded in `docs/evidence/phase1-rest-contract.md`.

A1 explicitly rejects workspace/state locking in V1. Concurrent local races are the caller's responsibility; implementations must not introduce advisory locks or collaboration semantics.

### 3.8 Portability and repository hygiene

First-party source, documentation, generated files, fixtures, and persisted artifact metadata must remain machine-independent.
They must not contain developer names, private project names, home-directory paths, or build-machine workspace roots.
Runtime output can show a resolved local path only in bounded `--full` detail when the user needs it.
Compact output and persisted metadata use logical workspace names and relative slash-delimited paths.
Captured upstream documentation and clearly isolated third-party fixtures are exempt, but implementation code must not copy their machine-specific examples.

## 4. CLI implementation evidence

This section contains implementation evidence only for CLI-owned registry rows. It does not restate delegated or excluded capabilities.

### Shared acceptance-test clauses

Behavioral tests are authored before implementation. Cards below reference these shared clauses:

| ID | Required acceptance behavior |
| --- | --- |
| **A-ID** | LUID succeeds; exact name/path succeeds when unique; zero/multiple matches fail without fuzzy or interactive fallback. |
| **A-PAGE** | Upstream pages/tokens normalize correctly; public output is bounded; continuation retrieves the next stable slice; catalog refresh exhausts pages internally. |
| **A-OUTPUT** | Explicit compact TOON allowlist by default, conditional `details: "--full"` disclosure, bounded expanded `--full`, explicit `--raw` only where supported, stable error/output fields, and separate compact/full golden fixtures. |
| **A-SECRET** | PAT names/secrets/tokens are absent from visible config values, output, logs, artifacts, catalog, fixtures, and diagnostics. |
| **A-LOCAL** | Canonical payload, metadata, view, baseline, external-edit detection, clean/dirty re-pull, and path-boundary behavior are tested. |
| **A-PREVIEW** | Mutation is the default when enabled; `--preview` performs no consequential mutation; the target resolves from complete provenance or explicit input; `--force` never authorizes execution. |
| **A-ASYNC** | Upload/job success, terminal failure, timeout, and request/job-ID reporting are deterministic. |
| **A-PARTIAL** | Multi-step operation stops on first failure, reports exact completed/failed work, and performs no rollback or automatic resume. |
| **A-META** | Metadata permission mode, ID mappings, pagination, warnings, and incomplete-result state are preserved rather than guessed or flattened. |
| **A-ERROR** | HTTP/upstream status, Tableau code/summary/detail, operation, target context, retryability, corrective action, and request/job ID are preserved with secret redaction. |

### 4.1 Foundation, configuration, catalog, workspace, and cross-content

| Capability(s) | Exact operation | Material contract | Variable behavior | Acceptance tests | Evidence / status |
| --- | --- | --- | --- | --- | --- |
| `env.profile.list/get/add/update/remove/set-default` | Read or atomically replace `<os.UserConfigDir>/tadx/config.yaml`. | YAML stores aliases, URL, site ID/content URL, auth type, PAT variable names, default environment/workspace, and non-secret settings. Never store PAT values. `auth status` reports resolved configuration/PAT-reference presence only. | No pagination or async behavior. The remote mutation execution policy does not affect these local writes. | `A-SECRET`; add collision; update exact alias; removing active default fails or explicitly changes it; equal update is exit-0 no-op; malformed YAML never leaves a partial file. | A1 §§7.2–7.4, 9.2; C1 §2.1. Architecture-locked. |
| `auth.check` | `POST /api/{version}/auth/signin`; optional `POST /api/{version}/auth/signout`. | Request carries PAT name, PAT secret, and `site.contentUrl`. Capture credential token, site LUID, and user LUID internally; expose only redacted status and exact target context. | Synchronous. Sign-in is authentication/session establishment, not a consequential resource mutation. Preserve TLS/proxy/auth/site errors and request IDs. | `A-SECRET`, `A-ERROR`; valid PAT/wrong site/expired PAT; token absent from output, logs, artifact, and catalog fixtures. | A1 §§5.4, 7.3–7.4; C1 §5.1; local official REST capture. Contract-verified in `docs/evidence/phase1-rest-contract.md`. |
| `auth.status` | Local config and environment-variable resolution only. | Return selected alias, auth type, PAT variable names, and whether referenced variables are present. Do not claim the PAT is valid or create a Tableau session. | No pagination/async behavior. | `A-SECRET`; missing variable vs missing profile; status remains usable offline. | A1 §§1.5, 5.4, 9.2; S1 requires a distinct row. Architecture-locked. |
| `capability.list`, `capability.get` | Query the executable capability registry used by command wiring/help. | Return stable ID, surface, owner, overlap, selectors, availability, local-write, remote-mutation, preview support, execution availability, and blocker. No separately maintained command tree or ownership map. | Bounded local continuation if needed. Mutation rows remain discoverable and report whether `TADX_ENABLE_MUTATIONS=1` enables execution. | Every public command has exactly one registry row; IDs unique; delegated rows resolve; all rows remain visible with mutation execution disabled; generated help matches registry. | A1 §§3.3, 5.2, 6.6; S1 single-source-of-truth correction. Architecture-locked. |
| `catalog.refresh` | Resource list/read endpoints plus only admitted focused Metadata API/permission reads. | Build a new generation with source environment/site, generated-at, counts, and completeness. Make it current only after all required pages/slices complete. Remote Tableau remains authoritative. | Exhaust upstream pagination internally. Metadata warnings or failed pages make the generation incomplete; never flatten to success. No generic retries. | `A-PAGE`, `A-META`, `A-ERROR`; failure on page N leaves prior complete generation current; source environment mismatch is visible; 12-hour stale threshold. | A1 §§5.9, 8.11, 11.2, 12.8; C1 §§5.3, 5.12. Docs-only remote facts. |
| `catalog.status` | Read local catalog generation metadata. | Return source, freshness, completeness, scope, and staleness without contacting Tableau. | No async behavior. | `A-OUTPUT`; stale warning at 12 hours; incomplete generation remains explicit. | A1 §§5.9, 8.11, 12.8; C1 §2.1. Architecture-locked. |
| `workspace.create/register/clone/list/status/move/artifact.delete/clean` | Local filesystem, the user configuration workspace registry, and workspace `tadx.yaml`/`.tadx/` state. | Each registration binds one case-insensitively unique logical name and stable ID to one unique machine-local root. Create and clone default to &lt;home&gt;/TADX/workspaces/&lt;name&gt;, and an explicit path overrides that default. Register requires an explicit existing root. Names follow portable path-safe rules across supported operating systems. Resolution order is explicit logical name -> containing registered workspace -> selected environment default -> general default -> fail. Persisted and rendered artifact paths are workspace-relative and slash-delimited. Create, clone, list, and status disclose registered roots only under `--full`. Layout contains canonical payload, `metadata.json`, `view.md`, optional `lineage.json`, and hidden state. | `artifact delete --preview` performs no deletion. Move and delete require explicit workspace and artifact selectors and atomically revalidate payload, metadata, and sidecars before commit. `clean` preserves managed canonical payloads by default. | `A-LOCAL`, `A-PREVIEW`; logical names and canonical roots are unique; no implicit workspace creation by pull; local move preserves Tableau ID; delete preview removes nothing; delete cannot escape the workspace; path traversal/collision fail; status detects external edits; concurrent changes stop move/delete; no lock files/coordination semantics. | A1 §§6.1, 6.3-6.4, 7.5-7.6, 8.1-8.2, ADR-032; C1 §2.1; maintainer deletion decision. Architecture-locked. |
| `search.run` | Existing bounded resource list adapters or the local normalized catalog index. | Search supported content, administration, and Pulse types with one output contract. Live Tableau is the default source; `--catalog` selects local SQLite without fallback. | Blank text requires a concrete type and uses that resource's list semantics. Broad and narrow type filters remain bounded. | `A-PAGE`, `A-ID`, `A-OUTPUT`; broad and narrow type filters; explicit unavailable catalog type; stable continuation; selected hit is re-resolved before mutation. | Architecture-locked shared search contract with hermetic action and adapter tests. |
| `lineage.pull` | Focused Metadata GraphQL reads after authoritative REST identity resolution. | Create a metadata-only artifact containing source provenance, lineage status, counts, and a relative `lineage.json` pointer. Keep Metadata IDs and REST LUIDs distinct. | Direct upstream and downstream reads are the default. Explicit direction, depth, and transitive traversal remain bounded. Warning-bearing or permission-limited results remain incomplete. | `A-ID`, `A-META`, `A-LOCAL`; direct graph; bounded depth; cycle deduplication; missing ID mapping; permission warning; relative paths; no native package or dependency download. | Contract-verified by the captured Metadata guide and schema plus hermetic tests recorded in `docs/evidence/lineage-metadata-contract.md`; live deployment verification is not claimed. |
| `doctor.run` | Local validators plus read-only Tableau sign-in and connectivity checks. | Return per-check pass/warn/fail, stable error ID, target context, and corrective action. Persistent logging remains off unless `TADX_LOG_LEVEL` is set. | No mutation; individual check failures do not suppress other independent checks. TADX never probes or reports Tableau MCP connection state. | `A-SECRET`, `A-ERROR`; offline config-only result; invalid PAT; stale catalog; dirty workspace. | A1 §§5.12, 7.7, 9.1; maintainer MCP ownership decision. Architecture-locked. |

### 4.2 Workbook

| Capability(s) | Exact operation | Material contract | Variable behavior | Acceptance tests | Evidence / status |
| --- | --- | --- | --- | --- | --- |
| `workbook.list` | `GET /api/{version}/sites/{site-id}/workbooks`. | Material inputs: endpoint-supported `pageNumber`, `pageSize`, `filter`, `sort`, `fields`. Return LUID, name/content URL, project, owner, and lifecycle fields used by selectors. | Classic REST pagination; C1 records common default 100 and maximum 1,000 where supported. | `A-PAGE`, `A-OUTPUT`, `A-ERROR`; endpoint-specific unsupported filter is not silently rewritten. | A1 §§1.5, 3.2; C1 §5.4. Docs-only. |
| `workbook.inspect` | `GET /api/{version}/sites/{site-id}/workbooks/{workbook-id}`. | Return authoritative workbook identity, project, owner, content URL, views/tags where supplied. | Synchronous exact read. | `A-ID`, `A-OUTPUT`; exact LUID and exact name/project path produce the same identity; ambiguity fails before call. | A1 §§5.7, 8.1; C1 §5.4. Docs-only. |
| `workbook.pull` | Workbook content GET; Metadata GraphQL POST; datasource exact/content GET when `--include-pds` is set. | Preserve `.twb` or `.twbx` bytes. Record explicit portability and direct published datasource REST LUIDs. Optional dependency acquisition preserves native `.tds` or `.tdsx` bytes as sibling artifacts and records workspace-relative paths without recursion. | Synchronous reads and one recoverable local bundle write. Clean workbook re-pull warns/replaces; dirty workbook stops unless `--overwrite`; dirty dependencies always stop acquisition. Incomplete optional detection writes `portability: unknown`; incomplete required detection stops before local writes. | `A-ID`, `A-LOCAL`, `A-ERROR`; native bytes preserved; complete Metadata pagination; duplicate and incomplete identities rejected; dirty packages protected; nested project provenance; transaction rollback and process-exit recovery; live Metadata LUID accepted by REST datasource get. | A1 §§6.3–6.4, 8.2, 8.6; C1 §§5.4, 5.13; official REST and Metadata captures. Contract-verified in `docs/evidence/phase1-rest-contract.md`; sanitized Tableau Cloud happy-path evidence closes B5. |
| `workbook.publish` | `POST /api/{version}/sites/{site-id}/workbooks`; large uploads use file-upload session endpoints. Optional internal TWB validation: `POST .../workbooks/validateWorkbook`. | Multipart request carries payload plus file/upload-session ID, workbook type, destination project, name, explicit overwrite, optional async/connection fields. A source-site-bound package remains unchanged. Return workbook identity or pollable job plus request/job IDs. | Mutation is the default; `--preview` stops before publish. Poll jobs internally to a bounded terminal result. Tableau remains authoritative for published-datasource reference validation. Standalone `workbook check` is not public. TWB validation applies only where supported. | `A-PREVIEW`, `A-ASYNC`, `A-ERROR`; preview makes no publish call; cross-site warning then preserved Tableau rejection; collision without overwrite; chunk order/session failure; terminal job failure; TWB validation warning/error; TWBX path does not claim server validation; unsupported validator follows optimistic-version policy. | A1 §§5.11, 6.5–6.9, 8.6, ADR-017/027; C1 §5.4; S1 workbook-check correction; local official REST capture. Contract-verified in `docs/evidence/phase1-rest-contract.md`. |
| `workbook.delete` | `DELETE /api/{version}/sites/{site-id}/workbooks/{workbook-id}`. | Preview the exact source environment, site, project, workbook name, and LUID. Re-resolve the planned LUID immediately before the final request. | Mutation is the default; `--preview` stops before deletion. Preserve Tableau's dependency, permission, missing-resource, and request-ID response. | `A-PREVIEW`, `A-ID`, `A-ERROR`; preview has no delete; renamed or moved target retains LUID identity; missing target and repeated delete remain explicit. | Exact official source and hermetic contract tests are recorded in `docs/evidence/phase1-rest-contract.md`. |

### 4.3 Datasource

| Capability(s) | Exact operation | Material contract | Variable behavior | Acceptance tests | Evidence / status |
| --- | --- | --- | --- | --- | --- |
| `datasource.list` | `GET /api/{version}/sites/{site-id}/datasources`. | Return LUID, name/content URL, project, owner, type, and lifecycle fields used by selectors. | Classic REST pagination; endpoint-specific filters/sorts only. | `A-PAGE`, `A-OUTPUT`, `A-ERROR`. | A1 §§1.5, 3.2; C1 §5.5. Docs-only. |
| `datasource.inspect` | `GET .../datasources/{id}`; focused `POST /api/metadata/graphql`; optional `POST /api/v1/vizql-data-service/read-metadata` and `/get-datasource-model`. | Base read returns identity/project/owner/type. Bounded detail can return stable ID mappings, names/captions, roles/types/aggregations, formulas, descriptions/inheritance, hidden/folder state, logical tables/relationships, and direct composition facts. | Metadata API connection cursor preferred, max page 1,000 per C1; never combine `after` with `offset`. Record permission mode. Preserve warnings such as backfill/node/time/page limits. VDS detail is version/product dependent. | `A-ID`, `A-PAGE`, `A-META`; metadata ID must not be guessed to equal REST LUID; permission modes yield explicitly different completeness; warning-bearing response is not reported as complete. | A1 §§8.3–8.4, 11.4; C1 §§5.5, 5.12. Docs-only. |
| `datasource.pull` | `GET /api/{version}/sites/{site-id}/datasources/{datasource-id}/content` plus focused metadata reads. | Preserve native `.tds`/`.tdsx` package and every direct published-parent reference needed for republish. Write provenance, baseline, human view, and explicit parent mapping. | Clean/dirty re-pull invariant applies. No generic dependency graph or transitive acquisition. | `A-LOCAL`, `A-ID`; ordinary datasource round-trip; multi-parent composed fixture pull→republish; missing/inaccessible parent diagnostics; package bytes/semantics not silently normalized. | Contract-verified in `docs/evidence/datasource-lifecycle-rest-contract.md`; composition authoring remains deferred. |
| `datasource.publish` | `POST /api/{version}/sites/{site-id}/datasources`; large uploads use file-upload sessions. Composed path sends `parentDataSourceUrls`. | Request includes file/upload-session ID, datasource type, explicit destination/name, and explicit overwrite/append/replace mode. For composed artifacts, send every serialized immediate parent and no transitive parents. | Mutation is the default; `--preview` stops before publish. Preserve unsupported version, parent access, connector, credential, depth/count, and connection failures. | `A-PREVIEW`, `A-ERROR`; ordinary create/overwrite; append/replace never inferred; large upload; one/multiple immediate parents; missing and extra parent URLs; old deployment error preserved. | Contract-verified in `docs/evidence/datasource-lifecycle-rest-contract.md`; composition authoring remains deferred. |
| `datasource.delete` | `DELETE /api/{version}/sites/{site-id}/datasources/{datasource-id}`. | Preview the exact source environment, site, project, datasource name, and LUID. Re-resolve the LUID immediately before the final request. | Mutation is the default; `--preview` stops before deletion. Preserve Tableau's workbook dependency, permission, missing-resource, and request-ID response. | `A-PREVIEW`, `A-ID`, `A-ERROR`; preview has no delete; depended-on datasource; missing target; permission failure; repeated delete behavior follows captured contract. | Contract-verified in `docs/evidence/datasource-lifecycle-rest-contract.md`. |

### 4.4 Flow

| Capability(s) | Exact operation | Material contract | Variable behavior | Acceptance tests | Evidence / status |
| --- | --- | --- | --- | --- | --- |
| `flow.list` | `GET /api/{version}/sites/{site-id}/flows`. | Return flow LUID, name, project, owner, and lifecycle fields used by selectors. | Classic REST pagination; product/license support is deployment-specific. | `A-PAGE`, `A-OUTPUT`; unsupported deployment surfaces upstream failure. | Contract-verified by the official REST capture and hermetic tests recorded in `docs/evidence/flow-rest-contract.md`; live deployment verification is not claimed. |
| `flow.inspect` | `GET /api/{version}/sites/{site-id}/flows/{flow-id}`. | Return exact identity, project, owner, output steps, and supported metadata. | Synchronous exact read. | `A-ID`, `A-OUTPUT`. | Contract-verified by the official REST capture and hermetic tests recorded in `docs/evidence/flow-rest-contract.md`; live deployment verification is not claimed. |
| `flow.pull` | `GET /api/{version}/sites/{site-id}/flows/{flow-id}/content`. | Preserve `.tfl`/`.tflx`; create canonical payload, provenance, human view, lineage sidecar when available, and baseline. Do not rewrite connections, credentials, published datasource bindings, files, or database references. | Clean/dirty re-pull invariant applies. | `A-LOCAL`, `A-ID`; both package types; permission failure; dirty protection; native bytes preserved. | Contract-verified by the official REST and Metadata captures plus hermetic tests recorded in `docs/evidence/flow-rest-contract.md` and `docs/evidence/lineage-metadata-contract.md`; live deployment verification is not claimed. |
| `flow.publish` | `POST /api/{version}/sites/{site-id}/flows`; upload sessions for large files. | Multipart request carries the unchanged TFL/TFLX or upload-session ID, type, explicit project/name/overwrite, and only endpoint-required fields. Tableau validates published datasource, file, database, and connection references. | Mutation is the default; `--preview` stops before publish. Do not rewrite flow semantics, connections, credentials, schedules, or linked tasks. | `A-PREVIEW`, `A-ERROR`; invalid package; collision; large upload; unsupported license/product; Tableau reference rejection preserved. | Contract-verified by the official REST capture and hermetic tests recorded in `docs/evidence/flow-rest-contract.md`; live deployment verification is not claimed. |
| `flow.move` | `PUT /api/{version}/sites/{site-id}/flows/{flow-id}` with an exact destination project field. | Preview exact flow identity and source and destination project LUIDs on one site. Do not change flow package contents, connections, credentials, schedules, or linked tasks. | Mutation is the default; `--preview` stops before movement. Cross-site movement is rejected. Equal destination is an exit-0 no-op after authoritative comparison. | `A-PREVIEW`, `A-ID`, `A-ERROR`; duplicate project leaf names; equal destination; destination permission failure; cross-site input rejected. | Contract-verified by the official REST capture and hermetic tests recorded in `docs/evidence/flow-rest-contract.md`; live deployment verification is not claimed. |
| `flow.delete` | `DELETE /api/{version}/sites/{site-id}/flows/{flow-id}`. | Preview the exact source environment, site, project, flow name, and LUID. Re-resolve the LUID immediately before the final request. | Mutation is the default; `--preview` stops before deletion. Preserve Tableau's dependency, permission, missing-resource, and request-ID response. | `A-PREVIEW`, `A-ID`, `A-ERROR`; preview has no delete; linked task or schedule constraint; missing target; permission failure; repeated delete behavior follows captured contract. | Contract-verified by the official REST capture and hermetic tests recorded in `docs/evidence/flow-rest-contract.md`; live deployment verification is not claimed. |

### 4.5 Project

| Capability(s) | Exact operation | Material contract | Variable behavior | Acceptance tests | Evidence / status |
| --- | --- | --- | --- | --- | --- |
| `project.list` | `GET /api/{version}/sites/{site-id}/projects`. | Return project LUID, name, parent identity/path inputs, description, and content-permission state needed by selectors. | Classic REST pagination. | `A-PAGE`, `A-OUTPUT`; nested projects with duplicate names preserve distinct IDs/paths. | Contract-verified by the official REST capture and hermetic tests recorded in `docs/evidence/project-rest-contract.md`; live deployment verification is not claimed. |
| `project.inspect` | Resolve exact LUID or exact slash-delimited path from authoritative project data. | Return one project only; path is a selector, not identity. | No fuzzy or interactive fallback. | `A-ID`; renamed/moved project old path fails; duplicate name across branches; exact LUID survives rename. | Contract-verified by the official REST capture and hermetic tests recorded in `docs/evidence/project-rest-contract.md`; live deployment verification is not claimed. |
| `project.create` | `POST /api/{version}/sites/{site-id}/projects`. | Request includes explicit name, description/content-permission fields, and parent ID only when selected. Return project LUID and authoritative parent/context. | Mutation is the default; `--preview` stops before creation; no inferred parent. | `A-PREVIEW`, `A-ID`; same-name collision; ambiguous parent; root vs explicit parent. | A1 §§6.5, 6.10; C1 §5.7. Docs-only. |
| `project.update` | `PUT /api/{version}/sites/{site-id}/projects/{project-id}`. | Only bounded metadata admitted in V1. Do not expose generic parent/hierarchy movement. | Mutation is the default; `--preview` stops before update; equal values no-op when detectable. | `A-PREVIEW`, `A-ID`; rename/description update; attempted parent move rejected as usage/capability error. | A1 §§6.5, 6.10 and remote-move exclusion; C1 §5.7. Docs-only. |
| `project.delete` | `DELETE /api/{version}/sites/{site-id}/projects/{project-id}`. | Delete the exact revalidated project and preserve Tableau's request evidence. | Mutation is the default; `--preview` stops before deletion; Tableau determines cascade behavior. | `A-PREVIEW`, `A-ID`, `A-ERROR`; exact target; missing project; permission failure; uncertain outcome. | Contract-verified in `docs/evidence/project-rest-contract.md`. |

### 4.6 Pulse

| Capability(s) | Exact operation | Material contract | Variable behavior | Acceptance tests | Evidence / status |
| --- | --- | --- | --- | --- | --- |
| `pulse.definition.list` | `GET /api/-/pulse/definitions`. | Return one bounded page with definition identity, datasource identity, configuration summary, and an opaque continuation token. | Tableau is the default source; `--catalog` performs a local-only read. | `A-PAGE`, `A-OUTPUT`; limit validation; cursor bound to target, limit, and source; malformed envelope and incomplete identity. | Live-verified on Tableau Cloud in `docs/evidence/pulse-live-contract.md`; hermetic client and action tests preserve the bounded contract. |
| `pulse.definition.inspect` | `GET /api/-/pulse/definitions/{definition_id}`. | Return one exact definition while preserving the released configuration document. | Tableau is the default source; `--catalog` performs a local-only read. | `A-ID`, `A-OUTPUT`; missing, mismatched, or incomplete identity; direct and enveloped response shapes. | Live-verified on Tableau Cloud in `docs/evidence/pulse-live-contract.md`; hermetic tests preserve exact identity and configuration. |
| `pulse.definition.pull` | Definition GET plus local artifact manager. | Write canonical `resource.json`, provenance metadata, a bounded view, and a baseline fingerprint. | Remote read is authoritative; dirty re-pull requires explicit overwrite. | `A-LOCAL`, `A-ID`; stable JSON canonicalization; dirty protection; path containment; remote missing. | Live-verified on Tableau Cloud in `docs/evidence/pulse-live-contract.md`; hermetic tests preserve the artifact contract. |
| `pulse.definition.create` | `POST /api/-/pulse/definitions` followed by bounded default-metric polling. | Compile bounded user intent into the proven request payload, validate exact raw datasource fields, and return definition and default-metric identities. | Mutation is the default; `--preview` stops before creation; an unresolved default metric preserves the created definition identity. Tableau remains authoritative for datasource eligibility. | `A-PREVIEW`, `A-ID`, `A-ERROR`; invalid field intent; exact name/datasource collision; modified plan; field drift; request media types; HTTP 201 identity; bounded polling. | Live-verified with one Pulse-eligible datasource on Tableau Cloud in `docs/evidence/pulse-live-contract.md`; hermetic tests preserve deterministic behavior. |
| `pulse.definition.delete` | `DELETE /api/-/pulse/definitions/{definition_id}`. | Resolve one exact definition before deletion and preserve Tableau's response. | Mutation is the default; `--preview` stops after the exact GET. Tableau remains authoritative for cascade and dependency behavior. | `A-PREVIEW`, `A-ERROR`; definition with metrics/subscriptions; repeated delete; permission failure. | Live-verified with HTTP 204 on Tableau Cloud in `docs/evidence/pulse-live-contract.md`; hermetic tests preserve error behavior. |
| `pulse.metric.list` | `GET /api/-/pulse/definitions/{definition_id}/metrics`. | Return one bounded definition-scoped page with exact metric identity and specification summary. | Tableau is the default source; `--catalog` performs a local-only read; continuation stays source-bound. | `A-PAGE`, `A-ID`, `A-OUTPUT`; missing definition; provider limit violation; incomplete or mismatched ownership. | Live-verified on Tableau Cloud in `docs/evidence/pulse-live-contract.md`; hermetic tests preserve bounded pagination. |
| `pulse.metric.inspect` | `GET /api/-/pulse/metrics/{metric_id}`. | Return one exact metric specification without substituting current value or insight output. Preserve unrecognized specification fields. | Tableau is the default source; `--catalog` performs a local-only read. | `A-ID`; missing, mismatched, or incomplete identity; direct and enveloped response shapes. | Live-verified on Tableau Cloud in `docs/evidence/pulse-live-contract.md`; hermetic tests preserve exact identity. |
| `pulse.metric.fork` | `POST /api/-/pulse/metrics:getOrCreate` plus bounded exact-read reconciliation. | Copy the source specification, apply only bounded timeframe or allowed-dimension changes, and preserve unrecognized fields. | Mutation is the default; `--preview` stops before mutation; return created or reused status; wait a bounded time for inventory visibility after verifying ownership. | `A-PREVIEW`, `A-ID`, `A-ERROR`; no-op fork; disallowed dimension; created flag required; definition, datasource, and site ownership mismatch; pending visibility. | Live-verified on Tableau Cloud in `docs/evidence/pulse-live-contract.md`; hermetic tests preserve deterministic reconciliation. |
| `pulse.metric.delete` | `DELETE /api/-/pulse/metrics/{metric_id}`. | Resolve one exact metric before deletion and preserve Tableau's response. | Mutation is the default; `--preview` stops after the exact GET. Tableau remains authoritative for dependency behavior. | `A-PREVIEW`, `A-ERROR`; subscribed metric; repeated delete; permission failure. | Live-verified with HTTP 204 on Tableau Cloud in `docs/evidence/pulse-live-contract.md`; hermetic tests preserve error behavior. |
| `pulse.metric.followers` | `GET /api/-/pulse/subscriptions?metric_id={metric_id}`. | Return bounded normalized user and group subscription relationships for one exact metric. | Tableau is the default source; `--catalog` performs a local-only read. | `A-ID`, `A-OUTPUT`; user/group shapes; incomplete subscription identity; mismatched metric; output bound. | Live-verified on Tableau Cloud in `docs/evidence/pulse-live-contract.md`; hermetic tests preserve normalized output. |
| `pulse.metric.follow` | `POST /api/-/pulse/subscriptions:batchCreate`. | Converge one exact user or group subscription using the captured batch-create request shape. | Mutation is the default; `--preview` stops before mutation; a proven duplicate response returns already-following. | `A-PREVIEW`, `A-ID`, `A-ERROR`; exactly one subscriber type; user/group body; duplicate response; entitlement failure. | Live-verified for one user subscription on Tableau Cloud in `docs/evidence/pulse-live-contract.md`; hermetic tests preserve convergence. |
| `pulse.metric.unfollow` | `DELETE /api/-/pulse/subscriptions/{subscription_id}`. | Delete one exact subscription, or resolve metric plus subscriber to exactly one relationship before preview. | Mutation is the default; `--preview` stops before deletion; selector-based mutation revalidates the same subscription identity. | `A-PREVIEW`, `A-ID`; zero, one, or multiple matches; identity drift; exact HTTP 204 response. | Live-verified with HTTP 204 on Tableau Cloud in `docs/evidence/pulse-live-contract.md`; hermetic tests preserve exact resolution. |

### 4.7 Users, groups, and permissions

| Capability(s) | Exact operation | Material contract | Variable behavior | Acceptance tests | Evidence / status |
| --- | --- | --- | --- | --- | --- |
| `admin.user.list` | `GET /api/{version}/sites/{site-id}/users`. | Return user LUID, username, full name/email, site role, auth setting, and supported status fields. | Classic REST pagination. | `A-PAGE`, `A-OUTPUT`, `A-SECRET`. | Contract-verified in `docs/evidence/admin-rest-contract.md`. |
| `admin.user.inspect` | `GET /api/{version}/sites/{site-id}/users/{user-id}`. | Return one authoritative site-user record. | Exact read. | `A-ID`; exact username/email ambiguity; deleted user. | Contract-verified in `docs/evidence/admin-rest-contract.md`. |
| `admin.user.create` | `POST /api/{version}/sites/{site-id}/users`. | Request includes explicit username, site role, auth setting, and only endpoint-supported fields. Return created site-user ID. | Mutation is the default; `--preview` stops before creation; no role/auth inference. | `A-PREVIEW`, `A-ERROR`; already-member, quota/license, invalid role/auth. | Contract-verified in `docs/evidence/admin-rest-contract.md`. |
| `admin.user.update` | `PUT /api/{version}/sites/{site-id}/users/{user-id}`. | Patch explicit supported attributes only; do not expand PAT-only TADX auth into password-management behavior. | Mutation is the default; `--preview` stops before update; equal authoritative values no-op. | `A-PREVIEW`, `A-ID`; role/license transition; equal update; unsupported field. | Contract-verified in `docs/evidence/admin-rest-contract.md`. |
| `admin.user.delete` | `DELETE /api/{version}/sites/{site-id}/users/{user-id}`. | Remove exact site user. Ownership reassignment is never implicit. | Mutation is the default; `--preview` stops before deletion; upstream ownership constraint is preserved. | `A-PREVIEW`, `A-ERROR`; user owns content; repeated removal; exact target. | Contract-verified in `docs/evidence/admin-rest-contract.md`. |
| `admin.group.list` | `GET /api/{version}/sites/{site-id}/groups`. | Return group ID/name and supported domain/import metadata. | Classic REST pagination. | `A-PAGE`, `A-OUTPUT`. | Contract-verified in `docs/evidence/admin-rest-contract.md`. |
| `admin.group.inspect` | Group exact resolution plus `GET /api/{version}/sites/{site-id}/groups/{group-id}/users` when membership requested. | Return one group and bounded direct membership. There is no separate public member-list capability. | Normalize group and member pagination separately. | `A-ID`, `A-PAGE`; duplicate group names; external/directory group membership limitations. | Contract-verified in `docs/evidence/admin-rest-contract.md`. |
| `admin.group.create` | `POST /api/{version}/sites/{site-id}/groups`. | Send explicit name and only endpoint-supported directory/import settings; return group ID. | Mutation is the default; `--preview` stops before creation. | `A-PREVIEW`, `A-ERROR`; duplicate name; unsupported directory setting. | Contract-verified in `docs/evidence/admin-rest-contract.md`. |
| `admin.group.update` | `PUT .../groups/{group-id}` plus membership pre-read, `POST .../groups/{group-id}/users`, and `DELETE .../groups/{group-id}/users/{user-id}`. | Public operation accepts explicit attribute changes and/or desired/add/remove user LUIDs. Preview exact added/removed/unchanged sets. | Mutation executes deterministic ordered calls; stop on first member failure; report completed/failed; no rollback/resume. Equal desired state is exit-0 no-op. | `A-PREVIEW`, `A-PARTIAL`, `A-ID`; one add/one remove; duplicate inputs normalized; second member failure; directory-managed restriction. | Contract-verified in `docs/evidence/admin-rest-contract.md`. |
| `admin.group.delete` | `DELETE /api/{version}/sites/{site-id}/groups/{group-id}`. | Delete exact group; users remain. Preview only cheaply authoritative direct facts and do not claim complete downstream permission impact. | Mutation is the default; `--preview` stops before deletion. | `A-PREVIEW`, `A-ERROR`; directory group; users retained; repeated delete. | Contract-verified in `docs/evidence/admin-rest-contract.md`. |
| `admin.permission.inspect` | Permission GET endpoints for exact workbook, datasource, flow, or project; project-default reads only where admitted. | Normalize principal type/ID, capability, Allow/Deny, resource, source endpoint, and direct/default/inherited/unknown classification. Do not claim a complete effective-permission evaluator. | Endpoint-specific paging/shape. Incomplete principal resolution is explicit. | `A-ID`, `A-META`; direct vs default; locked/inherited/unknown; inaccessible principal; compare normalized fixture to raw payload. | Contract-verified in `docs/evidence/admin-rest-contract.md`; source-code reuse review is not a blocker. |
| `admin.permission.create` | Permission PUT endpoints for an exact supported resource or project-default content kind. | Add one explicit capability and Allow/Deny mode for one exact user or group. | Mutation is the default; `--preview` stops before creation; identical rules are no-op; opposite explicit modes require deletion first. | `A-PREVIEW`, `A-ID`, `A-ERROR`; all resource kinds; user/group; direct/default; inherited source; drift; uncertain outcome. | Contract-verified in `docs/evidence/permission-mutations-rest-contract.md`. |
| `admin.permission.delete` | Permission DELETE endpoints for an exact supported resource or project-default content kind. | Delete one explicit capability and current mode for one exact user or group. | Mutation is the default; `--preview` stops before deletion; absent rules are no-op; mismatched explicit modes fail. | `A-PREVIEW`, `A-ID`, `A-ERROR`; all resource kinds; user/group; direct/default; inherited source; drift; uncertain outcome. | Contract-verified in `docs/evidence/permission-mutations-rest-contract.md`. |

## 5. Delegated, deferred, and rejected boundaries

| Boundary | Disposition / owner | Contract | Evidence |
| --- | --- | --- | --- |
| Datasource analytical query / VDS | Delegated to Tableau MCP | Use `query-datasource`. TADX does not expose a standalone query command or invoke MCP internally. | A1 §§3.2–3.4, 8.5, ADR-002/031; C1 §3 |
| Pulse current values, insight bundles, and briefs | Delegated to Tableau MCP | Use Pulse analytical MCP tools. TADX owns definitions/configuration, not analytical values. | A1 §§3.4, 8.5, ADR-023/031; C1 §3 |
| View/custom-view data and images | Delegated to Tableau MCP | Use view data/image tools; no TADX export/read command is admitted for parity. | A1 §§3.2–3.4; C1 §3 |
| Workbook semantic authoring/modification | Delegated to Tableau Desktop / Desktop MCP | TADX may pull/publish native artifacts but does not transform workbook XML to author content. | A1 §§1.6, 8.6; C1 §3 |
| Datasource field-description generation | Agent or selective skill | Reasoning and review can use MCP metadata/query results. TADX does not write field descriptions in V1. | A1 §§3.1, 4.4, 8.4; C1 §2.4 |
| Datasource composition authoring | Deferred indefinitely | TADX preserves ordinary and composed datasource packages but does not create or change composition relationships. | Maintainer decision |
| Datasource field-description updates | Deferred | TADX can inspect descriptions but does not expose a remote description mutation. | Maintainer decision |
| Project pull and publish | Deferred | TADX retains project list, inspect, create, and update without a shallow project package operation. | Maintainer decision |
| Pulse definition and metric updates | Deferred | Create a new definition or fork a metric instead of mutating an existing specification. | Maintainer decision |
| Flow authoring | Out of scope for TADX; Tableau Prep/product surface | Flow lifecycle is admitted; custom semantic authoring is not. | A1 §§1.6, 8.6 equivalent product boundary; C1 §2.5 |
| Generic bulk deletion, cleanup policy, recycle-bin recovery, and permanent purge | Deferred | V1 admits exact local artifact, workbook, datasource, flow, Pulse definition, and Pulse metric deletion only. Each delete supports `--preview`. | Maintainer deletion decision; A1 §§3.1–3.4, 6.5, 9.3 |
| TDS remote work-copy lifecycle, diff, and impact | Deferred fast follow | Wait for mature released work-copy management/listing. Pre-release behavior cannot enter V1 contracts. | A1 §§1.6, 13.3–13.5, ADR-030; C1 §7 |
| Unbounded lineage and dependency orchestration | Deferred | V1 admits bounded read-only lineage snapshots. It does not acquire transitive dependencies, choose migration order, or provide an unbounded graph engine. | Maintainer lineage decision; A1 §§8.9, 13.6, ADR-020; C1 §7 |
| Recursive project migration | Deferred | No child-project recursion, transitive dependency migration, rollback, or resume in V1. | A1 §§6.10, 13.6, ADR-021; C1 §7 |
| `pack` / `unpack` and Hyper ↔ CSV | Deferred | Native package expansion/rebuild and Hyper conversion move after V1. | A1 §§1.6, 13.1–13.2, ADR-029; C1 §7 |
| Generic bulk pull/publish | Deferred | No generic bulk or shallow project package engine is admitted. | A1 §§1.6, 8.12; C1 §7 |
| Generic remote content move / hierarchy migration | Rejected | V1 exposes exact resource-specific moves for workbooks, published datasources, flows, and project reparenting. TADX does not add a generic or recursive migration command. Local workspace move never mutates Tableau. | Maintainer lifecycle decision; A1 §§1.6, 8.1, 9.3; C1 §7 |
| Flow run, scheduling, linked-task administration, monitoring, cancellation, connection mutation, permission mutation, tags, governance, and migration keychains | Out of scope V1 | Flow lifecycle stays limited to list, inspect, pull, publish, move, delete, and bounded lineage. TADX does not become a Prep Conductor operations console. | Maintainer flow decision; A1 capability-admission rule; C1 §§2.5, 7 |
| Long-tail Tableau administration | Not admitted | Schedules, alerts, Bridge, connected apps, identity pools, site/server settings, collections, revisions, acceleration, extensions, and similar endpoints remain raw API/product territory absent a new admitted capability. | A1 §§1.1, 1.6, 9.3; C1 §§1, 7 |
| OAuth/JWT/UAT/Connected App auth | Deferred | PAT-only V1. | A1 §§1.5–1.6, 5.4, 7.3; C1 §7 |
| MCP proxy inside TADX | Rejected invariant | CLI and MCP are peer tools. No TADX verb invokes or proxies MCP. | A1 §§1.3, 3.2, ADR-002/031; C1 §7 |
| Plugin system, background daemon/sync, offline mutation queue, Tableau Next | Deferred or out of scope as specified by A1 | One modular-monolith binary; no hidden replay/background process; Tableau Next excluded. | A1 §§1.6, 2.1, ADR-001; C1 §7 |

## 6. Blocking verification and architecture conflicts

### 6.1 Blocking verification gates

No V1 registry row has a blocking verification gate.
Capabilities removed from V1 appear only in the deferred boundaries in section 5.
The sanitized live Tableau Cloud evidence for the closed B5 gate is recorded in `docs/evidence/phase1-rest-contract.md`.

### 6.2 Architecture conflicts and resolutions

| Finding | Resolution in this contract |
| --- | --- |
| C1 proposed lightweight per-workspace locking. | Rejected. A1 §7.6 and ADR-032 make concurrent local races the caller's responsibility. No advisory/file lock is part of V1. |
| C1 used one mutation flag for local writes and remote changes. | Resolved by independent `Local write`, `Remote mutation`, and `Supports --preview` fields. Discovery gating applies only to remote mutations. |
| C1 combined public operations in coarse IDs and promoted internal member/composition mechanics. | Every public verb has one operation-level ID. Group member list/add/remove and datasource serialization/mapping remain implementation mechanics unless represented by the explicit public rows. |
| C1 added standalone `workbook check` because a validation endpoint exists. | Not admitted as a public V1 row. Workbook publish preview may invoke TWB validation internally when available; independent command admission requires demonstrated workflow demand. |
| C1 assigned generic workbook/datasource deletion to MCP because `delete-content` exists. | Superseded. TADX V1 owns exact workbook, datasource, and flow deletion. Generic bulk deletion and cleanup policy remain deferred. |
| C1 external references `X1`–`X3` and supplied API aliases lack exact attached source artifacts. | Evidence is downgraded to docs-only. This is a reproducibility gap, not an architecture conflict. |

**Confirmed unresolved architecture conflict:** none.
Deferred capabilities do not block the V1 registry.
